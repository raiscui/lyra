# LATER_PLANS

## 2026-03-09 12:01:16 UTC

- depth anchor 的参数面已经预留了 `depth_anchor_source = baseline_render | dataset | external`, 但 V1 目前只真正实现了 `baseline_render`
- 如果后续继续推进第二阶段:
  - 优先评估 `dataset` / `external` source 的尺度对齐和 provenance
  - 再决定是否把 depth anchor reference 持久化进 resume state

## 2026-03-09 14:25:00 UTC

- 给 `FlashVSR-Pro` 补一个项目内可复用的启动脚本或 runbook 片段,自动设置:
  - `BLOCK_SPARSE_ATTN_CUDA_ARCHS=80`
  - `LD_LIBRARY_PATH=torch/lib`
  - 本机 `local-python`
- 给 `refine_robust_v2` 增加一个“读取 stage3sr ply 后自动做 `view 5` side-eval”的小工具,避免以后每次手工拼命令

## 2026-03-09 14:31:34 UTC

- 把 `docs/plans/2026-03-06-long-lrm-style-post-refinement.md` 重构成更清楚的三层:
  - 当前 continuation backlog
  - 已完成 / 已验证项
  - historical `Task 1 ~ Task 10`
- 同时显式标出:
  - 已弃用方向
  - 明确不在本计划内的范围

## 2026-03-09 14:45:00 UTC

- 当前已经完成第一轮文档结构收口
- 如果后续还要继续降噪, 下一步可以考虑:
  - 把 `Closest-to-SplatSuRe Track` 单独拆成二期计划文档
  - 把 `Task 1 ~ Task 10` 移到独立 historical archive 文档

## 2026-03-09 17:04:30 UTC

- 给 `diagnostics.json` 的 `stopped_reason` 增加对 `--stop-after` 的显式语义,避免当前这种:
  - 真实上是手动在 `stage2b` 截断
  - 但摘要仍回落成 `metrics_plateau`
- 补一条 `native -> Stage 2B` 的 `sub8` full-view 对照,与这轮 `SR -> Stage 2B` 直接并排比较
- 如果继续沿当前 SR 分支推进,优先从 `full_view_stage2b_from_sr_stage3sr_sub8_iter20_20260309_run2` 再追加一段 `Stage 2B` iter,因为最后 5 个点仍在稳定改善

## 2026-03-10 02:53:49 UTC

- 用户已经明确后续不再追加 `view 5` 侧视角补评, 因此此前“自动 side-eval helper”不再是当前优先项
- 如果 `Closest-to-SplatSuRe Track Phase A` 落地后仍然需要继续追 SR 主线, 下一优先级应当是:
  - `Phase B` 的全图 weighted SR supervision
  - 而不是再回头扫 `Task A` 的表层超参数

## 2026-03-10 03:11:30 UTC

- `Phase A` 新口径的 first full-view `sub8` rerun 已完成, 结果接近持平
- 如果继续沿这条主线推进, 下一轮优先只选一个方向深挖:
  1. 给 `WeightBuilder` 暴露 `ratio_threshold / fidelity_sigmoid_k / fidelity_min_views` 的 CLI 参数,做一轮小范围校准
  2. 直接进 `Phase B`, 把新 selection 从“单 patch 路由”推进到“更大覆盖面的 weighted SR supervision”
