# 笔记: 2026-03-09 add-refinement-v2-depth-anchor

## 2026-03-09 11:45:40 UTC 四文件摘要

- 任务目标:
  - 近期主线集中在 `refinement_v2` 的 stage 细化、外部 reference、以及 selective SR 后续路线
  - 本轮新变更是 `add-refinement-v2-depth-anchor`, 目标是把 baseline render depth 作为 self-anchor 接入 appearance 阶段
- 关键决定:
  - `Stage 2A` 不是几何纠偏阶段
  - baseline render depth anchor 的定位应是 anti-drift, 不是 geometry correction
  - 若继续推进 depth anchor, 优先复用 `Phase 0 / Phase 1` evaluation render, 不先改 dataloader depth 契约
- 关键发现:
  - 当前 `losses.py` 还没有 depth anchor loss
  - `gaussian_adapter.py` 当前让 `Stage 2A / Stage 3SR` 只动 `opacity/scales/colors`
  - `gs.py` 已能渲染 `depths_pred`
- 实际变更:
  - 旧六文件已因 `task_plan.md`、`WORKLOG.md` 超过 1000 行而续档为带时间戳版本
- 错误与根因:
  - 暂无本轮实现错误
- 可复用点候选:
  1. baseline render depth anchor 的语义边界, 要和 geometry correction 严格区分
  2. 六文件超 1000 行时, 应整体续档后再开始新任务, 避免上下文污染
  3. `refinement_v2` 后续高 observation 密度优化, 优先看 `render_meta` 的内存物化路径
- 是否需要固化到 docs/specs: 否, 当前已有 OpenSpec 变更文档承载本轮设计
- 是否提取/更新 skill: 否, 目前没有新增到足以独立成 skill 的跨项目新规律

## 2026-03-09 11:45:40 UTC 当前理解

- OpenSpec schema: `spec-driven`
- 进度: `0/13`
- 先做代码与测试路径核对, 再按任务清单逐项勾选

## 2026-03-09 12:01:16 UTC 实现要点

- 现象:
  - `refinement_v2` 之前只有 RGB / patch / geometry regularizer, 没有 depth anchor
  - 单卡与多卡路径其实都已经能拿到 `depths_pred`, 只是没有被消费
- 假设:
  - 只要把 immutable baseline reference 固定下来, 再把归一化后的 depth loss 接进共享 appearance loop, 就能满足 V1 需求
  - `Stage 2B` 只要完全不动, 就不会被这次改动污染
- 验证:
  - 新增 `tests/refinement_v2/test_depth_anchor.py`
  - 覆盖:
    1. depth normalization 的 scale-invariant 语义
    2. alpha threshold 对 valid mask 的过滤
    3. `Stage 2A -> Stage 3SR` 只捕获一次 reference
    4. 多设备 shard 路径也能计算 depth anchor
    5. `pred_depth_missing` / `empty_reference_mask` 都会降级但不中断
- 结论:
  - baseline_render depth anchor 已经真正接入 `Stage 2A / Stage 3SR`
  - `Stage 2B` 的 metrics 不会出现 `loss_depth_anchor`
  - CLI smoke 已验证参数和主流程连通

## 2026-03-09 12:01:16 UTC smoke 结果摘录

- 运行命令核心参数:
  - `scripts/refine_robust_v2.py`
  - `--enable-depth-anchor`
  - `--lambda-depth-anchor 0.25`
  - `--stop-after stage2a`
- 输出摘要:
  - `phase_reached = stage2a`
  - `stopped_reason = metrics_plateau`
  - `depth_anchor.enabled = true`
  - `depth_anchor.reference_ready = true`
  - `depth_anchor.reference_valid_ratio = 1.0`
  - `depth_anchor.last_skip_reason = null`
  - `stage2a.loss_depth_anchor = 0.0`

## 2026-03-09 12:15:30 UTC 真实 A/B 验证结果

- 现象:
  - 用户要求用真实 demo 产物 `outputs/demo/lyra_static/.../lyra_static_demo_generated` 跑 `refine_robust_v2`, 看是否有提升
  - 这次要回答的不只是“refine 会不会变好”, 还要看“depth anchor 是否带来额外收益”
- 实验设计:
  - 固定输入与数据资产
  - 只跑 `Stage 2A`
  - 两组都 `iters_stage2a=5`
  - A/B 唯一区别是是否打开 `--enable-depth-anchor --lambda-depth-anchor 0.25`
- 对照组结果:
  - baseline PSNR `17.9734`
  - final PSNR `19.7669`
  - PSNR gain `+1.7935`
  - residual_mean `0.08424 -> 0.06024`
  - sharpness `0.001903 -> 0.002088`
- depth-anchor 组结果:
  - baseline PSNR `17.9734`
  - final PSNR `19.4680`
  - PSNR gain `+1.4946`
  - residual_mean `0.08424 -> 0.06291`
  - sharpness `0.001903 -> 0.001940`
  - `loss_depth_anchor = 0.003518`
  - `depth_anchor_valid_ratio ≈ 1.0`
  - `depth_anchor_skip_reason = null`
- 差分:
  - final PSNR: depth-anchor 比 no-anchor `-0.2989`
  - final residual_mean: depth-anchor 比 no-anchor `+0.00267`
  - final sharpness: depth-anchor 比 no-anchor `-0.000148`
- 辅助观察:
  - 两张 `final_render_frame_0000.png` 确实不同
  - 用 PIL 做平均绝对像素差, `mean_abs_diff ≈ 1.73 / 255`
  - 说明 depth anchor 真在改变优化轨迹,不是“参数开了但没参与”
- 当前结论:
  - `refine_robust_v2` 本身在这份 demo 上是有效的
  - 但当前 5 iter / Stage 2A 口径下, depth anchor 没有继续拉高 RGB 指标, 反而更保守
  - 这和它的设计定位一致: 更像 anti-drift 约束, 不是为了短程 PSNR 冲榜

## 2026-03-09 12:28:40 UTC 第二轮 A/B: `target_subsample=8`, `iters_stage2a=20`

- 现象:
  - 用户要求把 observation 密度提到 `sub8`, 并增加迭代数
  - 需要验证“更长优化后 depth anchor 会不会开始体现收益”
- 实验设计:
  - 仍然只跑 `Stage 2A`
  - A/B 唯一区别继续保持为是否开启 depth anchor
- 对照组结果:
  - baseline PSNR `17.8840`
  - final PSNR `22.3538`
  - PSNR gain `+4.4698`
  - residual_mean `0.08508 -> 0.04232`
  - sharpness `0.001891 -> 0.002574`
- depth-anchor 组结果:
  - baseline PSNR `17.8840`
  - final PSNR `21.6990`
  - PSNR gain `+3.8150`
  - residual_mean `0.08508 -> 0.04598`
  - sharpness `0.001891 -> 0.002440`
  - `loss_depth_anchor = 0.00295`
  - `valid_ratio ≈ 1.0`
  - `skip_reason = null`
- 差分:
  - final PSNR: depth-anchor 比 no-anchor `-0.6548`
  - final residual_mean: depth-anchor 比 no-anchor `+0.00366`
  - final sharpness: depth-anchor 比 no-anchor `-0.000134`
- 辅助观察:
  - 两张最终渲染帧仍然确实不同
  - 平均绝对像素差约 `2.15 / 255`
- 当前结论:
  - 把迭代拉长到 `20`、observation 提到 `sub8` 后, depth anchor 依旧没有在 RGB 指标上反超
  - 当前证据更支持它是“保守的 anti-drift 约束”, 而不是直接追求更高 PSNR 的项

## 2026-03-09 12:40:10 UTC 权重扫描: `lambda_depth_anchor = 0.05 / 0.10 / 0.25`

- 现象:
  - 用户要求继续实验
  - 上一轮 `lambda=0.25` 明显更保守, 需要判断是否只是权重过大
- 实验设计:
  - 固定 `target_subsample=8`, `iters_stage2a=20`
  - 比较 `no-anchor`, `0.05`, `0.10`, `0.25`
- 结果:
  - no-anchor:
    - `final_psnr = 22.3538`
    - `final_residual_mean = 0.04232`
  - `lambda=0.05`:
    - `final_psnr = 22.1610`
    - `final_residual_mean = 0.04338`
    - `loss_depth_anchor = 0.00850`
  - `lambda=0.10`:
    - `final_psnr = 22.0298`
    - `final_residual_mean = 0.04411`
    - `loss_depth_anchor = 0.00551`
  - `lambda=0.25`:
    - `final_psnr = 21.6990`
    - `final_residual_mean = 0.04598`
    - `loss_depth_anchor = 0.00295`
- 差分相对 no-anchor:
  - `0.05`: `PSNR -0.1928`
  - `0.10`: `PSNR -0.3240`
  - `0.25`: `PSNR -0.6548`
- 辅助观察:
  - 最终帧相对 no-anchor 的平均绝对像素差:
    - `0.05`: `1.09 / 255`
    - `0.10`: `1.47 / 255`
    - `0.25`: `2.15 / 255`
- 当前结论:
  - 这条曲线非常接近“单调更保守”
  - 在当前口径下, `0.05` 是已测试权重里最温和、最接近 no-anchor 的一个
  - 但目前还没有出现“既保留 depth 约束, 又超越 no-anchor 的 sweet spot”

## 2026-03-09 13:12:00 UTC 侧视角验证入口确认

- 现象:
  - 用户强调的是“正面看不出, 侧面一转就暴露”的厚化歧义
  - 现有 `final_render.mp4` 只是对当前 `scene.cam_view` 渲染,并不自动等于额外 novel-view
- 已确认事实:
  - `GaussianSceneRenderer.render(...)` 最终仍然消费 `scene.cam_view`
  - `configs/demo/lyra_static.yaml` 注释已给出经验规律:
    - `view "3/4"(zoom) 通常更稳,重影更少`
    - `view "5"(clockwise/orbit) 更容易出现厚表面重影`
- 当前主假设:
  - 如果 depth anchor 真在抑制沿视线方向厚化,它最先应该在 `view 5` 这类 orbit 轨迹上出现迹象
- 备选解释:
  - depth anchor 只是整体减小了参数更新,所以任何视角下都只表现成“更保守”,不会对 `view 5` 特别有利
- 最小验证:
  - 用 `view_ids=5` 单独构造场景
  - 对 baseline / no-anchor / anchor 的最终高斯分别跑 `stop_after=phase0`
  - 对比单独的 `view 5` 指标与渲染帧

## 2026-03-09 13:20:00 UTC 口径纠偏: 当前实验未使用 FlashVSR-Pro 6 路 reference

- 现象:
  - 用户提醒当前实验可能漏掉了 `FlashVSR-Pro` 的 6 路超分视频
- 验证:
  - 已检查当前 A/B diagnostics 和命令口径
  - 当前 run 没有 `reference_root/reference_path`
  - 当前 run 没有 `reference_mode=super_resolved`
  - `outputs/flashvsr_reference` 目录为空
- 结论:
  - 之前的 A/B 是 native 监督口径
  - 不是“真实 external SR reference”口径
- 后续动作:
  - 先搜索本机是否已有别处生成好的 FlashVSR reference
  - 若不存在,再评估是补环境还是改机器执行

## 2026-03-09 13:50:00 UTC FlashVSR-Pro 环境打通记录

- 现象:
  - 当前机器上没有现成的 `outputs/flashvsr_reference/full_scale2x`
  - 用户明确要求“超分要重新生成”
- 验证过程:
  1. `/workspace/FlashVSR-Pro` 仓库与模型存在
  2. 初始缺失:
     - `imageio`
     - `block_sparse_attn`
     - 后续又确认缺 `ftfy`、`datasets`、`ffmpeg-python`、`pytorch-lightning`、`torchsde`
  3. `block_sparse_attn` 首次编译失败:
     - 动态证据: `nvcc fatal: Unsupported gpu architecture 'compute_120'`
     - 静态证据: `Block-Sparse-Attention/setup.py` 默认 `BLOCK_SPARSE_ATTN_CUDA_ARCHS=80;90;100;110;120`
  4. 修正策略:
     - 不改 repo 代码
     - 只在编译时设置 `BLOCK_SPARSE_ATTN_CUDA_ARCHS=80`
     - 适配 A800 / sm_80
  5. 结果:
     - `block_sparse_attn` 编译并安装成功
     - `infer.py --help` 在带 `LD_LIBRARY_PATH=torch/lib` 的环境下成功启动
- 当前结论:
  - FlashVSR-Pro 已具备在这台机器上重新生成 6 路 SR 视频的运行条件

## 2026-03-09 14:00:00 UTC FlashVSR 单路 smoke 结果

- 现象:
  - 之前没有现成 SR reference, 需要先证明当前机器真的能跑 `FlashVSR-Pro`
- 验证命令要点:
  - `scripts/run_flashvsr_reference.py`
  - `--runner local`
  - `--local-python /workspace/lyra/.pixi/envs/default/bin/python3`
  - `--view-ids 5 --scene-stem 00172`
  - 继承 `LD_LIBRARY_PATH=torch/lib`
- 动态证据:
  - 子进程链路里可见真实 `infer.py`
  - 输出视频已落盘:
    - `outputs/flashvsr_reference/full_scale2x/5/rgb/00172.mp4`
  - 最终脚本正常退出, 状态 `succeeded`
- 结论:
  - 本机已经具备批量生成剩余 5 路 SR reference 的条件

## 2026-03-09 14:15:00 UTC FlashVSR 6 路生成完成

- 验证结果:
  - `view 0/1/2/3/4/5` 的 `00172.mp4` 全部成功生成
  - `scripts/run_flashvsr_reference.py` 对单路与批量都返回 `status=succeeded`
  - 总汇总写入:
    - `outputs/flashvsr_reference/flashvsr_reference_summary.json`
- 当前结论:
  - 现在已经具备“真实 external SR reference”口径下重新跑 `refine_robust_v2` 的前提条件
- 下一步实验设计:
  - 先跑 no-anchor SR 基线
  - 再跑 `lambda_depth_anchor=0.05`
  - 之后优先比较 SR 口径下的指标变化与侧视角表现

## 2026-03-09 14:25:00 UTC SR 口径下的 depth-anchor 结论

- 现象:
  - 用户指出之前实验漏掉了 `FlashVSR-Pro` 6 路 SR 视频
  - 补齐 external SR reference 后, 需要重新回答 depth anchor 是否有帮助, 尤其是侧视厚化问题
- 验证:
  1. 重新生成 6 路 `FlashVSR-Pro` 2x 视频:
     - `outputs/flashvsr_reference/full_scale2x/<view>/rgb/00172.mp4`
  2. full-view SR no-anchor:
     - `phase_reached = stage3sr`
     - `final_psnr = 23.99824876297582`
     - `final_residual_mean = 0.03450822830200195`
     - `final_sharpness = 0.0029591515194624662`
  3. full-view SR `lambda_depth_anchor=0.05`:
     - `phase_reached = stage3sr`
     - `final_psnr = 23.730659749111137`
     - `final_residual_mean = 0.03565481677651405`
     - `final_sharpness = 0.0029060000088065863`
     - `loss_depth_anchor = 0.008658349514007568`
     - `reference_ready = true`
  4. `view 5` side eval:
     - no-anchor: `PSNR = 24.55829798589391`, `residual_mean = 0.03392832353711128`
     - lambda005: `PSNR = 24.332168589503556`, `residual_mean = 0.0349496454000473`
     - 逐帧 PSNR 差分 `lambda005 - no_anchor`:
       - `121/121` 帧全部为负
       - `delta_mean = -0.24434 dB`
       - 最差帧 `frame 65 = -0.32511 dB`
       - 最好帧 `frame 92 = -0.16214 dB`
  5. 已导出侧视对比板:
     - `outputs/refine_v2/side_eval_view5_sr_compare_board_20260309.png`
- 结论:
  - 用户担心的“FlashVSR 没接入”这一点已经纠正
  - 真实 SR 路径本身明显有效
  - 但在当前 demo / 当前权重 / 当前阶段配置下, depth anchor 依旧没有在侧视角上反超 no-anchor
- 当前更可信的定位仍然是“保守约束”, 不是“侧视几何救火器”

## 2026-03-09 14:31:34 UTC 计划文档盘点: `2026-03-06-long-lrm-style-post-refinement.md`

### 现象

- 用户指出 `depth anchor` 已弃用, 需要重新判断这份计划文档里到底还有哪些项没完成
- 该文档同时混有:
  - 当前状态说明
  - continuation tasks
  - historical 0-to-1 tasks
  - 更高侵入的二期路线

### 静态证据

- `Continuation Task A` 明确写有 `已完成真实系统验证`, 但保留后续优化方向:
  - `W_sr_select`
  - patch loss 配比
  - `L_sampling_smooth`
  - pruning / opacity 耦合
- `Continuation Task B` 明确写有 `第一版已经完成并做过真实资产验证`, 剩余是收口 full-view 基线
- `Continuation Task C` 没有出现 `Status (已完成)` 之类状态块, 内容仍是文档/命名收口要求
- `Continuation Task D` 明确写有默认衔接策略已基本定住, 剩余是策略收口
- 文档明确声明:
  - `Task 1 ~ Task 10` 是历史任务拆解
  - 不应再被理解成当前未完成清单
- `Current Continuation Order` 已把继续顺序改写为:
  - `B -> D -> A -> Closest-to-SplatSuRe Track`

### 结论

- 当前最明确仍未完成的主块:
  - `Continuation Task C`
  - `Closest-to-SplatSuRe Track` 的 Phase A-E
- 当前已落地但仍有继续优化空间的项:
  - `Continuation Task A`
  - `Continuation Task B`
  - `Continuation Task D`
- `depth anchor` 不是这份计划文档的 continuation 主线组成部分
  - 在用户已宣布弃用后, 不应再把它算成这份计划的剩余工作

## 2026-03-09 14:45:00 UTC 文档收口执行结果

### 实际修改

- 在计划文档前部新增了阅读指南
  - 先看 `Current Continuation Order`
  - 再看 `Continuation Tasks`
  - 再决定是否进入 `Closest-to-SplatSuRe Track`
  - 最后才看历史任务
- 给 `Continuation Task C` 增加了显式状态块
- 把 `Historical 0-to-1 Tasks` 改成带 `Archive Only, Not Current Backlog` 的标题
- 把 `Current Continuation Order` 改成带 `Use This As The Current Backlog Order` 的标题
- 在 `明确不在本计划内` 中加入:
  - `depth anchor`
  - 当前已弃用, 不作为 continuation item

### 当前判断

- 这轮修改已经解决"打开文档后先读到历史任务, 从而误判 backlog"的问题
- 但它仍然是同一份长文档
- 如果以后还要继续沿这条线推进, 更彻底的方案仍然是把:
  - 当前 backlog
  - 历史任务
  - 高侵入二期路线
  拆成独立文档或独立主章节

## 2026-03-09 15:11:02 UTC `Continuation Task C` 文档统一结果

### 修改范围

- `docs/cmd.md`
- `docs/joint_refinement_camera_gaussians_v2_usage.md`
- `docs/2026-03-06-rtx4090-baseline-task-a-runbook.md`

### 统一后的核心口径

- `stage2a`
  - 仍然是对外主线阶段名
- `Stage 3A / Phase 3S / Stage 3SR`
  - 是 `stage2a` 内部的增强子阶段
- `--stop-after stage2a`
  - 仍是正确写法
  - 在 enhanced 链路下, `diagnostics.json` 的 `phase_reached` 可以是 `stage3sr`
- `metrics_stage2a.json`
  - 记录的是对外 `Stage 2A` 主线中的 native cleanup 落盘
  - 不是独立 `stage3a` CLI 的证据

### 额外发现

- `docs/cmd.md` 原本存在明确漂移:
  - 文档上方写 full-view 正式 observation 预算是 `target_subsample=16`
  - 但 smoke 命令正文却还残留 `--target-subsample 8`
- 这类 drift 会直接把执行者带到错误的显存预算和错误结论

## 2026-03-09 15:25:00 UTC `Continuation Task B` 文档收口结果

### 统一后的核心口径

- full-view native smoke
  - 是当前 48G 主机上的正式 baseline 起点
  - 固定 `target_subsample=16`
  - 预期 `phase_reached = stage2a`
- full-view SR smoke
  - 是同 observation 预算下的增强分支
  - 也固定 `target_subsample=16`
  - 预期 `phase_reached = stage3sr`
- `Stage 2B`
  - 当前不属于 full-view baseline 的第一步
  - 应该放到 native / SR smoke 都稳定之后再讨论

### 对计划文档口径的影响

- `Continuation Task B` 现在不再该被理解成"入口还没打通"
- 更准确地说:
  - 入口已打通
  - 文档和命令第一轮已收口
  - 剩余工作是更长 smoke 和后续 `Stage 2B` 衔接

## 2026-03-09 16:27:00 UTC `SR -> Stage 2B` full-view sub8 启动慢的动态证据

### 现象

- `resume + start-stage stage2b` 的 full-view sub8 run 启动后, 长时间没有生成 `metrics_stage2b.json`
- 终端本身也没有即时日志输出

### 初始假设

- 候选假设 A: 进程可能卡在恢复状态或 `pixi` 启动阶段
- 备选假设 B: 它其实已经在跑, 只是 full-view 前置计算很慢

### 验证

- `ps -ef` 显示实际 Python 进程持续高占用运行
- 手动 `Ctrl-C` 后拿到栈:
  - `runner.run()`
  - `run_phase1_prepare_weights()`
  - `weight_builder.build_weight_map()`
  - `torch.quantile(...)`
- 同时 `state/latest.pt` 时间戳已被刷新

### 结论

- 上面的候选假设 A 不成立
- 当前更可信的结论是:
  - full-view `sub8` 在 `start-stage=stage2b` 之前, 仍然会重新跑 `Phase 0 + Phase 1`
  - 其中 `Phase 1` 的 full-view residual quantile / weight map 构造比较重
- 因此“长时间没有 `metrics_stage2b.json`”不能直接解释成流程卡死

## 2026-03-09 17:02:00 UTC `sub8` full-view `SR stage3sr -> Stage 2B` 真实结果

### 运行命令口径

- 输入目录:
  - `outputs/refine_v2/full_view_stage2b_from_sr_stage3sr_sub8_iter20_20260309_run2`
- 关键参数:
  - `--target-subsample 8`
  - `--start-stage stage2b`
  - `--resume`
  - `--stop-after stage2b`
  - `--iters-stage2b 80`
  - `--reference-root outputs/flashvsr_reference/full_scale2x`
  - `--reference-mode super_resolved`
  - `--stage2a-mode enhanced`
  - `--patch-size 256`
  - `--lambda-patch-rgb 0.25`
  - `--lambda-sampling-smooth 0.0005`

### full-view 对比

- warm-start 起点 `stage3sr` 末值:
  - `PSNR = 23.996732275883772`
  - `residual_mean = 0.03452381119132042`
  - `sharpness = 0.002959481906145811`
- `Stage 2B` 80 iter 终点:
  - `PSNR = 25.40440766503693`
  - `residual_mean = 0.028707541525363922`
  - `sharpness = 0.004525278694927692`
  - `scale_tail_ratio = 0.023858189582824707`
  - `opacity_lowconf_ratio = 0.8734460473060608`
- 直接相对 `stage3sr` 末值的变化:
  - `PSNR +1.4076753891531588`
  - `residual_mean -0.005816269665956497`
  - `sharpness +0.0015657967887818813`
  - `scale_tail_ratio -0.05532316863536835`

### `view 5` 侧视角 dry-run 对比

- `stage3sr`:
  - `PSNR = 24.550691172149307`
  - `residual_mean = 0.03379035368561745`
  - `sharpness = 0.002946617780253291`
- `stage2b`:
  - `PSNR = 25.50232354666671`
  - `residual_mean = 0.029528144747018814`
  - `sharpness = 0.004487233702093363`
- 侧视角变化:
  - `PSNR +0.951632374517402`
  - `residual_mean -0.004262208938598633`
  - `sharpness +0.0015406159218400717`

### 额外观察

- `metrics_stage2b.json` 最后 5 个点仍在持续改善:
  - `25.3643 / 0.0288582 / 0.0044875`
  - `25.3725 / 0.0288207 / 0.0044967`
  - `25.3861 / 0.0287770 / 0.0045059`
  - `25.3940 / 0.0287502 / 0.0045138`
  - `25.4044 / 0.0287075 / 0.0045253`
- 因此当前不能把 `diagnostics.json` 里的 `stopped_reason = metrics_plateau` 解释成真的 plateau 证据
- 更像是 `stop_after` 早停场景下的默认摘要文案

## 2026-03-10 00:05:00 UTC `Continuation Task A` 进一步收缩后的结论

### 已完成的最小实验

1. `lambda_patch_rgb: 0.25 -> 0.5`
2. `no-pruning`
3. `W_sr_select` naive unit-peak 归一化(代码改动后真实跑 full-view)

### 动态证据

- baseline SR:
  - `selection_mean = 0.1455942988395691`
  - `selection_max = 0.25`
  - `PSNR = 23.996732275883772`
  - `residual_mean = 0.03452381119132042`
- patch_rgb=0.5:
  - `selection_mean = 0.14558736979961395`
  - `selection_max = 0.25`
  - `PSNR = 23.97708518225879`
  - `residual_mean = 0.034678343683481216`
- no-pruning:
  - `selection_mean = 0.1495991051197052`
  - `selection_max = 0.25`
  - `PSNR = 23.871456867765204`
  - `residual_mean = 0.034995175898075104`
- naive unit-peak normalization:
  - `selection_mean = 0.5823740363121033`
  - `selection_max = 1.0`
  - `PSNR = 23.93311310890023`
  - `residual_mean = 0.03498927131295204`

### 结论

- 当前 `Task A` 上的“轻量调参 + naive selection 动态范围放大”都没有把 full-view SR baseline 顶上去
- 这说明问题已经不太像:
  - 单纯 patch loss 比例不对
  - 单纯 pruning 压制过强
  - 单纯把 selection map 峰值放大就能解决
- 当前更可信的方向是:
  - 现有 fidelity / selection 本体就还是过于代理化
  - 需要转入 `Closest-to-SplatSuRe Track Phase A`
  - 用更真实的跨视图 `r_min / r_max / argmax_view / num_times_seen` 来重做 fidelity score

## 2026-03-10 03:02:00 UTC `Closest-to-SplatSuRe Track Phase A` 入口核对

### 已观察到的事实

- 当前 `phase3s` 真实入口在:
  - `src/refinement_v2/runner.py:1478`
- 当前 fidelity 仍是第一版 render-meta 代理:
  - `visible_mask`
  - `tiles_per_gauss` 归一化
  - `opacity_gate`
  - 再按 `max_view` 聚合成 per-Gaussian score
- 当前 `build_sr_selection_weight()` 会把低 fidelity Gaussian 投到所有可见视角,
  还没有利用 `argmax_view / max_view_mask`.
- `src/rendering/gs.py` 已经把 `radii / means2d / opacities / tiles_per_gauss` 向上抬到了 runner 层,
  因此 Phase A 这一步不需要再改 renderer.

### 当前主假设

- 如果把 fidelity 改成更真实的跨视图半径统计:
  - `r_min`
  - `r_max`
  - `rho = r_max / r_min`
  - `num_times_seen`
- 并且让 selection map 只在该 Gaussian 的 `max_view_mask` 上投影,
  那么当前 `phase3s` 就会更接近文档里要的 Phase A.

### 最小实现计划

1. 在 `weight_builder.py` 新增 fidelity diagnostics 结构:
   - `fidelity_score`
   - `r_min`
   - `r_max`
   - `rho`
   - `num_times_seen`
   - `argmax_view`
   - `max_view_mask`
2. 让 `compute_gaussian_fidelity_score()` 退化成 wrapper, 保持现有调用兼容
3. 让 `build_sr_selection_weight()` 消费 `max_view_mask`, 不再向所有可见视角散射
4. 在 `runner.py` / `diagnostics.py` 把额外统计写出, 至少补:
   - `rho_mean`
   - `num_times_seen_mean`
   - `max_view_counts`
5. 先补测试, 再改实现

## 2026-03-10 03:04:07 UTC `Closest-to-SplatSuRe Track Phase A` 实现结果

### 代码变更

- `src/refinement_v2/weight_builder.py`
  - 新增 `compute_gaussian_fidelity_diagnostics()`
  - fidelity 从单轮 proxy 改成跨视图:
    - `r_min`
    - `r_max`
    - `rho`
    - `num_times_seen`
    - `argmax_view`
    - `max_view_mask`
  - `build_sr_selection_weight()` 新增 `fidelity_diagnostics` 输入,并优先只向 `max_view_mask` 视角散射 low-fidelity Gaussian
- `src/refinement_v2/runner.py`
  - `phase3s` 改成先拿 fidelity diagnostics, 再写回 `metrics` 与 artifacts
- `src/refinement_v2/diagnostics.py`
  - `gaussian_fidelity_histogram.json` 新增:
    - `rho_*`
    - `num_times_seen_*`
    - `max_view_counts`

### 验证结果

- 针对性测试:
  - `PYTHONPATH="$(pwd)" pixi run pytest -q tests/refinement_v2/test_weight_builder.py tests/refinement_v2/test_patch_supervision.py -k "fidelity or max_view"`
  - 结果: `5 passed, 12 deselected`
- 回归测试:
  - `PYTHONPATH="$(pwd)" pixi run pytest -q tests/refinement_v2/test_weight_builder.py tests/refinement_v2/test_losses.py tests/refinement_v2/test_patch_supervision.py tests/refinement_v2/test_runner_phase0.py tests/refinement_v2/test_runner_stage2b.py`
  - 结果: `35 passed`

### 当前结论

- `Phase A` 这轮已经从“文档目标”进入“代码可运行状态”
- 但它目前还是实现级完成, 还不是实验级完成
- 要判断它是否真的把 SR 主线拉起来, 下一步必须回到 full-view `sub8` 的真实实验

## 2026-03-10 03:11:30 UTC 新 `Phase A` 口径的 full-view `sub8` `stage3sr` 真实结果

### 运行目录

- `outputs/refine_v2/full_view_sr_stage3sr_phaseA_rho_sub8_iter20_20260310`

### 现象

#### `stage3sr` 终点对比

- 旧 baseline:
  - `PSNR = 23.996732275883772`
  - `residual_mean = 0.03452381119132042`
  - `sharpness = 0.002959481906145811`
  - `loss_patch_rgb = 0.005008552223443985`
  - `loss_sampling_smooth = 0.00498782517388463`
- 新 `Phase A`:
  - `PSNR = 24.00097032415988`
  - `residual_mean = 0.03442930802702904`
  - `sharpness = 0.0029551826883107424`
  - `loss_patch_rgb = 0.0006426049512811005`
  - `loss_sampling_smooth = 0.0007339792209677398`
- 变化:
  - `PSNR +0.00423804827610752`
  - `residual_mean -0.00009450316429138184`
  - `sharpness -0.000004299217835068703`

#### `phase3s` 诊断对比

- 旧 baseline:
  - `sr_selection_mean = 0.1455942988395691`
  - `fidelity_mean = 0.0442831851541996`
  - `selection_max = 0.25`
- 新 `Phase A`:
  - `sr_selection_mean = 0.015738628804683685`
  - `fidelity_mean = 0.3613118827342987`
  - `selection_max = 0.9956401586532593`
  - `rho_mean = 0.9604628086090088`
  - `num_times_seen_mean = 52.867916107177734`
  - `max_view_counts` 已写入 `gaussian_fidelity_histogram.json`

### 结论

- `Phase A` 的代码改动确实改变了 `selection` 的空间分布:
  - 更尖锐
  - 更稀疏
  - 峰值更高
- 但从 full-view `sub8` 的最终 `stage3sr` 指标看, 当前收益基本接近持平
- 因此下一步更像要在两条路里选一条:
  1. 继续调 `Phase A` 的 `ratio_threshold / k / min_views` 等口径
  2. 直接进入 `Phase B`, 让更真实的 selection 去驱动更大覆盖面的 SR 监督
