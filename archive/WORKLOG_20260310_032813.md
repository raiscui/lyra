# WORKLOG

## [2026-03-09 12:01:16 UTC] 任务名称: 实现 add-refinement-v2-depth-anchor

### 任务内容
- 按 OpenSpec 变更 `add-refinement-v2-depth-anchor` 为 `refinement_v2` 实现 baseline render depth anchor
- 覆盖 `Stage 2A / Stage 3SR` 的 normalized depth consistency loss、diagnostics 和 graceful fallback
- 补充单测、回归测试与一条真实 CLI smoke

### 完成过程
- 在 `src/refinement_v2/config.py` 中新增 depth anchor 参数面:
  - `enable_depth_anchor`
  - `lambda_depth_anchor`
  - `depth_anchor_alpha_threshold`
  - `depth_anchor_source`
- 在 `src/refinement_v2/losses.py` 中新增:
  - `build_depth_anchor_valid_mask(...)`
  - `normalize_depth(...)`
  - `compute_depth_anchor_loss(...)`
- 在 `src/refinement_v2/runner.py` 中:
  - 新增 `DepthAnchorReference` 缓存
  - 只在 `Stage 2A / Stage 3SR` 捕获并复用 baseline reference
  - 单卡 / 多卡 appearance loop 都接入 `loss_depth_anchor`
  - diagnostics 会记录 `loss_depth_anchor`、`depth_anchor_valid_ratio`、`depth_anchor_source`、`depth_anchor_skip_reason`
  - `Stage 2B` 保持完全不接入 depth anchor
- 新增 `tests/refinement_v2/test_depth_anchor.py`, 覆盖:
  - depth normalization / mask filtering / 数值稳定性
  - reference 只捕获一次
  - `Stage 3SR` 复用 reference
  - multi-device shard 路径
  - `pred_depth_missing` / `empty_reference_mask` fallback
- 回写 `openspec/changes/add-refinement-v2-depth-anchor/tasks.md`, 13 项任务全部勾选

### 总结感悟
- 这次最关键的不是把 loss 写出来, 而是把 reference 的“不可变语义”守住
- 如果把 raw depth/alpha 塞进现有 `baseline` metrics 容器, 很容易把 Stage 2B gate 和最终 summary 一起污染
- 共享 `_run_appearance_stage(...)` 这条主循环, 是这次最干净也最稳的接线点

## [2026-03-09 12:15:30 UTC] 任务名称: 用真实 demo baseline 做 depth anchor A/B 验证

### 任务内容
- 使用 `outputs/demo/lyra_static/static_view_indices_fixed_5_0_1_2_3_4/lyra_static_demo_generated/gaussians_orig/gaussians_0.ply`
- 对 `refine_robust_v2` 做两组同口径 `Stage 2A` run:
  - no-anchor
  - depth-anchor
- 对比真实指标是否有提升

### 完成过程
- 共同命令口径:
  - `target_subsample=16`
  - `iters_stage2a=5`
  - `stop_after=stage2a`
  - 同一组 `pose/intrinsics/rgb root`
- 输出目录:
  - `outputs/refine_v2/ab_depth_anchor_no_anchor_20260309`
  - `outputs/refine_v2/ab_depth_anchor_with_anchor_20260309`
- 指标结果:
  - no-anchor:
    - `PSNR 17.9734 -> 19.7669`
    - `residual_mean 0.08424 -> 0.06024`
  - depth-anchor:
    - `PSNR 17.9734 -> 19.4680`
    - `residual_mean 0.08424 -> 0.06291`
    - `loss_depth_anchor = 0.003518`
    - `valid_ratio ≈ 1.0`
- 额外检查:
  - 尝试用 `imgdiff` 做帧差分时发现当前机器未安装该工具
  - 已改用 PIL 做平均绝对像素差统计,确认两组输出并非完全相同

### 总结感悟
- 这轮实验说明:
  - `refine_robust_v2` 本身会显著改善 baseline
  - 但 depth anchor 在短程 `Stage 2A` A/B 中并不自动等于更高的 RGB 指标
- 这更支持之前的定位:
  - 它是 anti-drift / anti-thickening 约束
  - 不是为了在少量迭代下直接把 PSNR 再往上冲

## [2026-03-09 12:28:40 UTC] 任务名称: 提高到 `sub8 + 20 iter` 后再次验证 depth anchor

### 任务内容
- 在同一份 demo baseline 上, 把 A/B 口径升级到:
  - `target_subsample=8`
  - `iters_stage2a=20`
- 继续比较 no-anchor 与 depth-anchor

### 完成过程
- 对照组输出目录:
  - `outputs/refine_v2/ab_depth_anchor_sub8_iter20_no_anchor_20260309`
- 实验组输出目录:
  - `outputs/refine_v2/ab_depth_anchor_sub8_iter20_with_anchor_20260309`
- 显存观察:
  - no-anchor 峰值运行期约 `63.5 GiB`
  - with-anchor 运行期约 `66.4 GiB`
  - 两组都未 OOM
- 指标结果:
  - no-anchor:
    - `PSNR 17.8840 -> 22.3538`
    - `residual_mean 0.08508 -> 0.04232`
  - with-anchor:
    - `PSNR 17.8840 -> 21.6990`
    - `residual_mean 0.08508 -> 0.04598`
    - `loss_depth_anchor = 0.00295`
    - `valid_ratio ≈ 1.0`

### 总结感悟
- 提高 observation 密度和迭代数后, depth anchor 依旧没有自动转化成更好的 RGB 指标
- 这说明它至少在当前 V1 口径下, 更像“限制漂移的刹车”, 而不是“拉高 PSNR 的油门”

## [2026-03-09 12:40:10 UTC] 任务名称: 在 `sub8 + 20 iter` 下扫描 depth anchor 权重

### 任务内容
- 固定同一 demo baseline 与 `Stage 2A` 口径
- 继续比较:
  - no-anchor
  - `lambda_depth_anchor=0.05`
  - `lambda_depth_anchor=0.10`
  - `lambda_depth_anchor=0.25`

### 完成过程
- 新增运行目录:
  - `outputs/refine_v2/ab_depth_anchor_sub8_iter20_lambda005_20260309`
  - `outputs/refine_v2/ab_depth_anchor_sub8_iter20_lambda010_20260309`
- 结果汇总:
  - no-anchor:
    - `PSNR = 22.3538`
  - `lambda=0.05`:
    - `PSNR = 22.1610`
  - `lambda=0.10`:
    - `PSNR = 22.0298`
  - `lambda=0.25`:
    - `PSNR = 21.6990`
- 观察到的趋势:
  - 权重越大, 最终输出越偏离 no-anchor
  - 权重越大, RGB 指标越保守
  - 同时 raw `loss_depth_anchor` 会下降,说明优化器确实在更强地服从 depth 约束

### 总结感悟
- 现在已经不是“0.25 太大所以看起来差”这么简单
- 更准确的说法是:
  - 在当前 demo 和 Stage 2A 口径里, depth anchor 会系统性地把优化拉回更保守的区域
  - 如果后续还想保留它, 优先应把它当成几何稳态约束来验证, 而不是继续拿它和 no-anchor 拼短程 PSNR

## [2026-03-09 14:25:00 UTC] 任务名称: 重新生成 FlashVSR-Pro 6 路 reference 并在真实 SR 口径下复核 depth anchor

### 任务内容
- 修正之前遗漏 `FlashVSR-Pro` external SR reference 的实验口径
- 重新生成 `00172` 的 6 路 `FlashVSR-Pro` 2x 视频
- 用真实 SR reference 重新跑 `refine_robust_v2` 的 no-anchor / depth-anchor A/B
- 回到 `view 5` 侧视轨迹验证厚化问题是否改善

### 完成过程
- 先排查并打通 `FlashVSR-Pro` 本机运行环境:
  - 在 `pixi` 环境中补齐 `ftfy`、`datasets`、`ffmpeg-python`、`pytorch-lightning`、`torchsde`
  - 编译 `block_sparse_attn`
  - 通过 `LD_LIBRARY_PATH=torch/lib` 解决运行时动态库加载
- 真实生成了 6 路 2x SR 视频:
  - `outputs/flashvsr_reference/full_scale2x/0..5/rgb/00172.mp4`
- 使用 external SR reference 重新跑 full-view enhanced 路径:
  - no-anchor 输出目录:
    - `outputs/refine_v2/ab_depth_anchor_sr_sub8_iter20_no_anchor_20260309`
  - depth-anchor 输出目录:
    - `outputs/refine_v2/ab_depth_anchor_sr_sub8_iter20_lambda005_20260309`
- 继续把两条最终 `gaussians_stage3sr.ply` 拉到 `view 5` 上做 `phase0` 侧视评估
- 生成了逐帧对比板:
  - `outputs/refine_v2/side_eval_view5_sr_compare_board_20260309.png`

### 总结感悟
- 这轮最重要的不是再扫更多 `lambda`, 而是先把 reference 口径纠正过来
- `FlashVSR-Pro` 真实接入后, external SR 路线本身确实能显著提升结果
- 但 depth anchor 在真实 SR 口径下依旧没有显出“侧视几何更薄”的证据, 说明它当前更像稳定器, 不是直接改善厚化的主因

## [2026-03-09 14:31:34 UTC] 任务名称: 盘点 `2026-03-06-long-lrm-style-post-refinement.md` 的剩余事项

### 任务内容
- 通读计划文档, 区分当前真正未完成项、已完成但仍可继续优化项、以及历史任务
- 结合用户已声明的 `depth anchor` 弃用状态, 避免把失效方向混进当前待办

### 完成过程
- 重新核对了文档中的:
  - `Continuation Task A / B / C / D`
  - `Closest-to-SplatSuRe Track`
  - `Current Continuation Order`
  - `Historical 0-to-1 Tasks`
- 确认这份文档不是单纯待办列表, 而是混合型状态文档
- 输出了一份新的判断口径:
  - 明确未完成:
    - `Continuation Task C`
    - `Closest-to-SplatSuRe Track`
  - 基本完成但还有收尾:
    - `Continuation Task A`
    - `Continuation Task B`
    - `Continuation Task D`
  - 不应再算当前待办:
    - `Task 1 ~ Task 10`

### 总结感悟
- 长计划文档一旦同时承载"历史拆解"和"当前状态", 很容易让后续执行者把历史任务重新误读成今天的待办
- 对这种文档, 最有价值的不是继续往下堆新段落, 而是及时把"已完成 / 剩余 / 历史"三层语义显式拆开

## [2026-03-09 14:45:00 UTC] 任务名称: 重构 `2026-03-06-long-lrm-style-post-refinement.md` 的阅读结构

### 任务内容
- 直接修改计划文档本身
- 让它能一眼区分:
  - 当前 backlog
  - 已完成但仍可优化
  - 历史归档
  - 不在计划内 / 已弃用方向

### 完成过程
- 在文档前部新增了 `Reader Guide / Current Backlog Snapshot`
- 显式写清:
  - `Continuation Task C` 是当前最明确未完成项
  - `Continuation Task B / D / A` 是后续优化项
  - `Closest-to-SplatSuRe Track` 是高侵入未来路线
  - `Task 1 ~ Task 10` 不应再作为当前待办
  - `depth anchor` 已弃用, 不纳入这份计划的 continuation backlog
- 给 `Continuation Task C` 补了 `Status (2026-03-09 refresh)`
- 把关键标题改成更清楚的语义:
  - `Historical 0-to-1 Tasks (Archive Only, Not Current Backlog)`
  - `Current Continuation Order (Use This As The Current Backlog Order)`

### 总结感悟
- 这轮没有新增任何技术方案, 但显著降低了后续执行时的认知噪音
- 对长计划文档来说, "怎么读"的结构提示, 有时和技术内容本身一样重要

## [2026-03-09 15:11:02 UTC] 任务名称: 落地 `Continuation Task C` 的文档统一

### 任务内容
- 统一阶段命名解释
- 统一 `stop_after` 与 `phase_reached` 的理解
- 统一 diagnostics / 产物对应关系
- 修正命令手册中已经漂移的 full-view `sub16` observation 口径

### 完成过程
- 更新了:
  - `docs/cmd.md`
  - `docs/joint_refinement_camera_gaussians_v2_usage.md`
  - `docs/2026-03-06-rtx4090-baseline-task-a-runbook.md`
- 在命令手册中新增了一段短的命名速记
- 在主 usage 文档中新增:
  - `误区 5`
  - `phase_reached` 解释
  - 扩展版 `Stage 2A` 映射表
  - 产物出现条件表
- 在 runbook 中明确:
  - `--stop-after stage2a` 与 `phase_reached = stage3sr` 是同一套增强链路的正常表现
- 用 `git diff --check` 做了文档格式校对

### 总结感悟
- 这轮真正修掉的不是一段文字, 而是一种高频误读路径
- 文档里的"口径漂移"一旦碰到显存预算或阶段语义, 会直接演化成错误实验设计

## [2026-03-09 15:25:00 UTC] 任务名称: 收口 `Continuation Task B` 的 full-view baseline 文档口径

### 任务内容
- 把 full-view baseline 的正式推荐顺序写清楚
- 统一 full-view native smoke / SR smoke 的职责
- 在计划文档里把 `Continuation Task B` 的剩余项从"入口打通"收敛成"更长 smoke / `Stage 2B`"

### 完成过程
- 更新了:
  - `docs/cmd.md`
  - `docs/joint_refinement_camera_gaussians_v2_usage.md`
  - `docs/plans/2026-03-06-long-lrm-style-post-refinement.md`
- 在命令速查里新增:
  - full-view 正式推荐顺序
  - 当前不推荐的做法
- 在主 usage 文档里新增:
  - full-view baseline 的 4 步顺序
  - native / SR smoke 的职责说明
  - 不要在 baseline 未固定前直接混入 `Stage 2B` 的提醒
- 在计划文档里给 `Continuation Task B` 增加了 `2026-03-09 refresh`

### 总结感悟
- 当一条路线已经"代码通了", 下一步最容易卡住的反而是"到底该按哪套命令和顺序执行"
- 这轮文档收口的价值, 在于把 `Continuation Task B` 从"开发态任务"压缩成了"实验态任务"

## [2026-03-09 17:03:30 UTC] 任务名称: `sub8` full-view `SR stage3sr -> Stage 2B` 验证与 warm-start mixed-device 修复

### 任务内容
- 在用户指定的 `--target-subsample 8` 口径下, 从 full-view `stage3sr` 结果继续做 `Stage 2B` warm-start
- 用真实结果验证是否继续提升
- 同时补强 `view 5` 侧视角证据
- 修复过程中暴露出的 warm-start mixed-device bug

### 完成过程
- 复制 `full_view_sr_stage3sr_long_sub8_iter20_20260309` 到独立 `run2` 目录, 避免污染 baseline
- 首轮真实命令先后暴露了两处 mixed-device 问题:
  - `build_patch_intrinsics()` 只对齐 dtype, 未对齐 device
  - `sr_selection_map` 缺失时的 fallback patch 权重留在 CPU
- 分别补了两条 CUDA 回归测试, 然后在 `src/refinement_v2/runner.py` 修复根因
- 回归测试通过后, 重新完成了 full-view `Stage 2B` 80 iter 实跑
- 再补了 `view 5` 单视角 `--dry-run` 对比, 直接检查侧视角收益

### 总结感悟
- `resume` workflow 不只要恢复高斯和 diagnostics, 还必须容忍某些运行期缓存缺失
- 这次最关键的不是单纯看到 full-view 平均指标上涨, 而是 `view 5` 侧视角也同步改善了, 这更贴近用户真正关心的几何问题

## [2026-03-10 02:53:49 UTC] 任务名称: 收口 `Continuation Task A` 并切换到 `Closest-to-SplatSuRe Track Phase A`

### 任务内容
- 把 `Task A` 这一轮三条失败实验的证据正式落盘
- 明确当前不再继续扫表层超参数
- 把主线切换到 `Closest-to-SplatSuRe Track Phase A`

### 完成过程
- 核对了三条 full-view `sub8` 实验的真实结果:
  - `lambda_patch_rgb: 0.25 -> 0.5`
  - `no-pruning`
  - naive `W_sr_select` unit-peak normalization
- 确认三条实验都没有让 SR baseline 变好, 且最后一条虽然把 `selection_max` 从 `0.25` 拉到 `1.0`, 依旧带来了指标退化
- 同步确认用户口径已经变化:
  - 后续统一 `--target-subsample 8`
  - 不再继续 `view 5` 侧视角补评
  - 当前是继续 backlog, 不是继续 `Stage 2B` 训练
- 基于这些证据, 把下一步正式收敛为:
  - 继续 `Closest-to-SplatSuRe Track Phase A`
  - 优先实现跨视图 fidelity score, 而不是继续微调 `Task A`

### 总结感悟
- 当 `selection_max` 被简单放大却仍然没有收益时, 问题往往已经不在“力度不够”, 而在“监督对象选错了”
- 这轮最重要的产出不是某个新参数, 而是用反证把错误方向尽快关掉, 让后续实现重新回到 fidelity 本体

## [2026-03-10 03:04:07 UTC] 任务名称: 落地 `Closest-to-SplatSuRe Track Phase A` 第一轮最小实现

### 任务内容
- 把 `phase3s` 的 fidelity 从 render-meta 代理推进到跨视图半径统计版
- 让 selection map 开始消费 `max_view_mask`
- 补齐对应测试和 diagnostics 产物

### 完成过程
- 先在 `tests/refinement_v2/test_weight_builder.py` 与 `tests/refinement_v2/test_patch_supervision.py` 写失败测试,
  锁住三类新行为:
  - `r_min / r_max / rho / num_times_seen`
  - `max_view_mask` 限制 selection 投影视角
  - `phase3s` 产物新增跨视图统计
- 在 `src/refinement_v2/weight_builder.py` 新增 `compute_gaussian_fidelity_diagnostics()`,
  并让 `compute_gaussian_fidelity_score()` 退化成兼容 wrapper
- 在 `build_sr_selection_weight()` 中引入 `fidelity_diagnostics`, 让 low-fidelity Gaussian 优先只投到自己的 `max_view_mask`
- 在 `src/refinement_v2/runner.py` 与 `src/refinement_v2/diagnostics.py` 把这些 diagnostics 接到 `phase3s` 指标与 `gaussian_fidelity_histogram.json`
- 完成后跑了两轮验证:
  - 针对性 `fidelity / max_view` 测试 `5 passed`
  - 相关回归 `35 passed`

### 总结感悟
- 这轮真正的变化不是“分数公式换了一行”, 而是把 selection 的作用对象从“所有可见 view”收缩到了“最该负责该 Gaussian 的 view”
- 这让 `Phase A` 第一次具备了和文档相符的结构语义, 但实验价值仍然要靠下一轮 full-view `sub8` 真实跑数来证明

## [2026-03-10 03:11:30 UTC] 任务名称: 用新 `Phase A` fidelity 口径重跑 full-view `sub8` `stage3sr`

### 任务内容
- 在不改命令口径的前提下, 用新 `Phase A` 代码重跑 full-view `sub8` `stage3sr`
- 与旧 baseline 对比, 判断这轮 fidelity / max-view routing 是否带来真实收益

### 完成过程
- 复用了旧 baseline 的 full-view `sub8` 参数, 只让代码中的 `Phase A` 实现发生变化
- 新输出目录:
  - `outputs/refine_v2/full_view_sr_stage3sr_phaseA_rho_sub8_iter20_20260310`
- 对比发现:
  - `stage3sr` 终点只有极小幅改善:
    - `PSNR +0.00424`
    - `residual_mean -0.0000945`
    - `sharpness -0.00000430`
  - 但 `phase3s` 诊断发生了明显重排:
    - `selection_mean 0.145594 -> 0.015739`
    - `selection_max 0.25 -> 0.995640`
    - `fidelity_mean 0.044283 -> 0.361312`
    - patch / sampling smooth 损失显著下降

### 总结感悟
- 这轮说明 `Phase A` 的最小实现已经真正改到了“监督路由”层, 而不是只改一层统计输出
- 但它还没有自动转化成显著指标收益, 这意味着后续要么继续校准 `Phase A`, 要么就要进入 `Phase B`, 让新的 selection 覆盖更大的监督面积
