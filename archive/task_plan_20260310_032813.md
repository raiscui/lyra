# 任务计划: add-refinement-v2-depth-anchor 实施

## 2026-03-09 11:45:40 UTC

### 目标

- 按 OpenSpec 变更 `add-refinement-v2-depth-anchor` 完成 `refinement_v2` 的 baseline depth anchor 实现
- 保持 `Stage 2B` 和 geometry-moving 路径不受 V1 depth anchor 影响
- 用测试和最小 smoke 验证功能、回退路径与多阶段复用语义

### 阶段

- [x] 阶段1: 续档六文件并做持续学习摘要
- [ ] 阶段2: 阅读 OpenSpec 上下文与相关代码
- [ ] 阶段3: 实现配置、reference 缓存与 loss 接线
- [ ] 阶段4: 补充测试并完成验证
- [ ] 阶段5: 回写任务状态与交付记录

### 关键问题

1. baseline depth reference 应该在 `Phase 0` 还是 `Phase 1` 捕获, 才能保证 `Stage 3SR` 复用同一份参考
2. 现有多卡 / shard 渲染路径里, `depths_pred` 与 reference mask 的设备和切片语义是否已经足够复用
3. graceful fallback 应该只记录 skip reason, 还是同时暴露 diagnostics 方便排查

### 做出的决定

- 决定1: 先按 OpenSpec 任务顺序实现, 但会在真正改代码前先把 reference 采集点和阶段边界读透
- 决定2: 先保守沿用 baseline render self-anchor 的定位, 不把这次实现扩成几何纠偏方案
- 决定3: 六文件已在本轮开始前续档, 新文件只记录本次变更实现过程

### 当前状态

- 正在读取 OpenSpec 规格文件和 `refinement_v2` 相关模块, 准备进入实现

## 2026-03-09 11:57:20 UTC

### 进展更新

- [x] 阶段2: 阅读 OpenSpec 上下文与相关代码
- [ ] 阶段3: 实现配置、reference 缓存与 loss 接线
- [ ] 阶段4: 补充测试并完成验证
- [ ] 阶段5: 回写任务状态与交付记录

### 已确认的实现边界

- `depths_pred` 在单卡和多卡路径里都已经能拿到, 多卡聚合入口在 `runner.py` 的 `_render_scene_multi_device(...)`
- `Stage 2A` 与 `Stage 3SR` 都统一走 `_run_appearance_stage(...)`, 因此 depth anchor 最稳的接线点也在这里
- baseline reference 更适合在 appearance stage 真正开始前按需捕获一次, 而不是在 `Phase 0` 无条件保存
- `Stage 2B` 现有循环完全独立, 只要不碰 `run_stage2b()` 就不会被新逻辑污染

### 接下来要做什么

- 先改 `config.py` / `losses.py` / `runner.py`
- 然后补充 `losses` 单测和 `runner` 回归测试

## 2026-03-09 12:01:16 UTC

### 完成更新

- [x] 阶段3: 实现配置、reference 缓存与 loss 接线
- [x] 阶段4: 补充测试并完成验证
- [x] 阶段5: 回写任务状态与交付记录

### 已完成内容

- `config.py`
  - 新增 depth anchor 的开关、权重、alpha 阈值、reference source
- `losses.py`
  - 新增 `build_depth_anchor_valid_mask(...)`
  - 新增 `normalize_depth(...)`
  - 新增 `compute_depth_anchor_loss(...)`
- `runner.py`
  - 新增 immutable baseline depth anchor 缓存
  - 在 `Stage 2A / Stage 3SR` 接入 depth anchor
  - 新增 `loss_depth_anchor` / valid ratio / skip reason diagnostics
  - 保持 `Stage 2B` 不接入 depth anchor
- `tests/refinement_v2/test_depth_anchor.py`
  - 补齐 loss 单测、single/multi-device 回归、reference 复用测试、graceful fallback 测试
- `openspec/.../tasks.md`
  - 13 个任务项已全部勾选

### 验证结果

1. `python3 -m py_compile src/refinement_v2/config.py src/refinement_v2/losses.py src/refinement_v2/runner.py tests/refinement_v2/test_depth_anchor.py`
   - 通过
2. `env PYTHONPATH="$(pwd)" pytest -q tests/refinement_v2/test_depth_anchor.py`
   - 结果: `9 passed`
3. `env PYTHONPATH="$(pwd)" pytest -q tests/refinement_v2/test_depth_anchor.py tests/refinement_v2/test_runner_phase0.py tests/refinement_v2/test_runner_stage2a.py tests/refinement_v2/test_runner_stage2b.py tests/refinement_v2/test_patch_supervision.py`
   - 结果: `34 passed`
4. `env PYTHONPATH="$(pwd)" pixi run python3 scripts/refine_robust_v2.py ... --enable-depth-anchor --lambda-depth-anchor 0.25 --stop-after stage2a`
   - 结果: CLI smoke 成功结束
   - `phase_reached = stage2a`
   - `depth_anchor.reference_ready = true`
   - `depth_anchor.reference_skip_reason = null`
   - `metrics_stage2a.json` / `diagnostics.json` / `videos/final_render.mp4` 已落盘

### 当前状态

- 本轮 `add-refinement-v2-depth-anchor` 实现与验证已完成, 正在准备最终交付说明

## 2026-03-09 12:10:00 UTC

### 新任务: 用 `outputs/demo/lyra_static/.../lyra_static_demo_generated` 做真实 A/B 验证

### 目标

- 用用户指定的 demo baseline 产物运行 `refine_robust_v2`
- 对比“关闭 depth anchor”与“开启 depth anchor”两条同配置 run
- 判断这次改动是否带来可观测提升,至少覆盖:
  - baseline -> final 的单次提升
  - no-anchor vs depth-anchor 的最终差异

### 阶段

- [ ] 阶段A: 确定可控的 A/B 命令口径
- [ ] 阶段B: 运行 no-anchor 对照组
- [ ] 阶段C: 运行 depth-anchor 实验组
- [ ] 阶段D: 提取 diagnostics / metrics 做对比
- [ ] 阶段E: 记录结论与边界

### 实验设计

- 采用同一输入:
  - `outputs/demo/lyra_static/static_view_indices_fixed_5_0_1_2_3_4/lyra_static_demo_generated/gaussians_orig/gaussians_0.ply`
- 为了隔离 depth anchor 影响:
  - 只跑 `Stage 2A`
  - `--stop-after stage2a`
  - 不引入 patch supervision
- 为了在有限时间内拿到可比较结果:
  - 使用中等迭代数而不是默认 600 轮

### 当前状态

- 正在确定这次 A/B 的具体命令与输出目录

## 2026-03-09 12:15:30 UTC

### 实验完成更新

- [x] 阶段A: 确定可控的 A/B 命令口径
- [x] 阶段B: 运行 no-anchor 对照组
- [x] 阶段C: 运行 depth-anchor 实验组
- [x] 阶段D: 提取 diagnostics / metrics 做对比
- [x] 阶段E: 记录结论与边界

### 运行口径

- 输入 baseline:
  - `outputs/demo/lyra_static/static_view_indices_fixed_5_0_1_2_3_4/lyra_static_demo_generated/gaussians_orig/gaussians_0.ply`
- 共同参数:
  - `configs/demo/lyra_static.yaml`
  - `scene_stem=00172`
  - `view_ids=5,0,1,2,3,4`
  - `target_subsample=16`
  - `iters_stage2a=5`
  - `stop_after=stage2a`
- 唯一区别:
  - 对照组: 不开 depth anchor
  - 实验组: `--enable-depth-anchor --lambda-depth-anchor 0.25`

### 结论摘要

- 两组 run 都比原始 baseline 有提升
- 但在这轮 5 iter 的 `Stage 2A` A/B 里:
  - no-anchor 的最终 RGB 指标更好
  - depth-anchor 组没有在 `PSNR / residual_mean / sharpness` 上带来额外收益
- 同时也确认:
  - depth anchor 确实生效了,不是因为 skip 才“看起来没影响”
  - `reference_ready = true`
  - `valid_ratio ≈ 1.0`
  - `loss_depth_anchor = 0.0035`

### 当前状态

- 正在整理面向用户的最终实验结论,会明确区分“refine_robust_v2 本身有效”与“depth anchor 在这轮 A/B 中是否额外增益”

## 2026-03-09 12:18:30 UTC

### 新任务: 提高 observation 密度与迭代数后再次做 A/B

### 目标

- 在更接近真实 refinement 的口径下重新比较 no-anchor 与 depth-anchor
- 检查上一轮 `sub16 + 5 iter` 的“更保守”结论, 在 `sub8 + 更多 iter` 下是否会改变

### 阶段

- [ ] 阶段A2: 固定新的 A/B 口径
- [ ] 阶段B2: 运行 `target_subsample=8` 的 no-anchor 对照组
- [ ] 阶段C2: 运行 `target_subsample=8` 的 depth-anchor 实验组
- [ ] 阶段D2: 提取并对比 diagnostics / metrics
- [ ] 阶段E2: 更新结论

### 实验设计

- 继续保持:
  - 同一 baseline `.ply`
  - 只跑 `Stage 2A`
  - 不引入 patch supervision
- 调整为:
  - `target_subsample=8`
  - `iters_stage2a=20`

### 当前状态

- 正在启动新的 no-anchor 对照组

## 2026-03-09 12:28:40 UTC

### 第二轮 A/B 完成更新

- [x] 阶段A2: 固定新的 A/B 口径
- [x] 阶段B2: 运行 `target_subsample=8` 的 no-anchor 对照组
- [x] 阶段C2: 运行 `target_subsample=8` 的 depth-anchor 实验组
- [x] 阶段D2: 提取并对比 diagnostics / metrics
- [x] 阶段E2: 更新结论

### 关键结果

- no-anchor:
  - `PSNR 17.8840 -> 22.3538`
  - `residual_mean 0.08508 -> 0.04232`
  - `sharpness 0.001891 -> 0.002574`
- with-anchor:
  - `PSNR 17.8840 -> 21.6990`
  - `residual_mean 0.08508 -> 0.04598`
  - `sharpness 0.001891 -> 0.002440`
  - `loss_depth_anchor = 0.00295`
  - `valid_ratio ≈ 1.0`
  - `skip_reason = null`

### 当前结论

- 现象:
  - 把口径提高到 `sub8 + 20 iter` 后, 两组都明显优于 baseline
  - 但 depth-anchor 组依然没有在 RGB 指标上追平 no-anchor
- 已验证结论:
  - depth anchor 仍然真实参与了优化, 不是 skip 掉了
  - 在当前 demo / Stage 2A / weight=0.25 这套口径下, 它更像一个保守约束, 而不是更高 PSNR 的捷径

### 当前状态

- 正在整理最终答复, 会把 `sub16 + 5 iter` 与 `sub8 + 20 iter` 两轮结果一起给用户

## 2026-03-09 12:31:20 UTC

### 新任务: 在 `sub8 + 20 iter` 下扫描 depth anchor 权重

### 目标

- 沿着刚才的真实 A/B 继续排查
- 判断 depth anchor 的“更保守”现象是否主要来自 `lambda_depth_anchor=0.25` 过大
- 在同一口径下比较:
  - no-anchor
  - `lambda_depth_anchor=0.05`
  - `lambda_depth_anchor=0.10`
  - `lambda_depth_anchor=0.25`(已有结果,用于对照)

### 阶段

- [ ] 阶段A3: 固定权重扫描口径
- [ ] 阶段B3: 运行 `lambda=0.05`
- [ ] 阶段C3: 运行 `lambda=0.10`
- [ ] 阶段D3: 汇总与 no-anchor / `lambda=0.25` 的对比
- [ ] 阶段E3: 更新结论

### 实验设计

- 继续保持:
  - `target_subsample=8`
  - `iters_stage2a=20`
  - `stop_after=stage2a`
  - 同一 baseline `.ply`
- 本轮只改 `lambda_depth_anchor`

### 当前状态

- 正在先跑 `lambda_depth_anchor=0.05`

## 2026-03-09 12:40:10 UTC

### 权重扫描完成更新

- [x] 阶段A3: 固定权重扫描口径
- [x] 阶段B3: 运行 `lambda=0.05`
- [x] 阶段C3: 运行 `lambda=0.10`
- [x] 阶段D3: 汇总与 no-anchor / `lambda=0.25` 的对比
- [x] 阶段E3: 更新结论

### 关键结果

- no-anchor:
  - `final_psnr = 22.3538`
  - `final_residual_mean = 0.04232`
- `lambda=0.05`:
  - `final_psnr = 22.1610`
  - 相对 no-anchor: `-0.1928`
- `lambda=0.10`:
  - `final_psnr = 22.0298`
  - 相对 no-anchor: `-0.3240`
- `lambda=0.25`:
  - `final_psnr = 21.6990`
  - 相对 no-anchor: `-0.6548`

### 趋势判断

- 已观察到:
  - `lambda` 越大, 输出和 no-anchor 的像素差越大
  - `lambda` 越大, `PSNR / residual_mean / sharpness` 越保守
- 已验证结论:
  - 在当前 demo / `sub8 + 20 iter` / `Stage 2A` 口径下, `0.05` 是已测试权重里最接近 no-anchor 的一个
  - 但它仍未超过 no-anchor

### 当前状态

- 正在整理最终建议, 会直接给出“如果现在要继续用 depth anchor, 应该先选哪个权重”

## 2026-03-09 13:05:00 UTC

### 新任务: 验证 depth anchor 是否改善侧视角的厚化/重叠歧义

### 目标

- 不再只看输入视角上的 `PSNR / residual_mean`
- 转向验证用户关心的几何歧义现象:
  - 正视角里重叠在一起看不出来
  - 侧视角或小幅 orbit 后, 会暴露出沿视线方向的厚化、双层、拉丝
- 比较 baseline / no-anchor / depth-anchor 在 novel-view 下的差异

### 阶段

- [ ] 阶段NV1: 定位现有 novel-view 渲染入口与最小可复用命令
- [ ] 阶段NV2: 选择对比对象并导出侧视角渲染结果
- [ ] 阶段NV3: 提取侧视角证据, 包括 RGB 与可行时的 depth 可视化
- [ ] 阶段NV4: 基于证据更新结论, 明确 depth anchor 是否真的抑制厚化

### 现象 / 假设 / 验证计划

- 现象:
  - 现有 A/B 只证明了 depth anchor 会让优化更保守
  - 但还没有回答它是否减少了“正面重叠, 侧面暴露”的厚化问题
- 当前假设:
  - depth anchor 可能不会提升输入视角 PSNR
  - 但可能减少沿视线方向被拉厚的高斯层, 因而在侧视角更稳定
- 备选解释:
  - depth anchor 只是整体抑制了更新幅度, 并没有针对性改善几何厚化
- 最小验证:
  - 复用当前 renderer 对 baseline / no-anchor / anchor 做同轨迹侧视角渲染
  - 对比侧面局部是否出现更少双层、拖尾、厚片感

### 当前状态

- 正在定位 novel-view 渲染链与已有脚本入口

## 2026-03-09 13:12:00 UTC

### 阶段NV1 完成更新

- [x] 阶段NV1: 定位现有 novel-view 渲染入口与最小可复用命令
- [ ] 阶段NV2: 选择对比对象并导出侧视角渲染结果
- [ ] 阶段NV3: 提取侧视角证据, 包括 RGB 与可行时的 depth 可视化
- [ ] 阶段NV4: 基于证据更新结论, 明确 depth anchor 是否真的抑制厚化

### 已确认事实

- `src/refinement_v2/runner.py` 的 `final_render` 仍然渲染 `scene.cam_view`, 不是额外构造的 novel camera
- `configs/demo/lyra_static.yaml` 已明确写出:
  - `view "5"(clockwise/orbit) 更容易出现厚表面重影`
- 当前最小可证伪实验不是先写新脚本造轨迹,而是先把 `view 5` 单独抽出评估
- 由于 `SceneBundle` 会把不同 `view_ids` 按顺序拼接,因此先前 A/B 的总指标掺杂了 `0/1/2/3/4` 这些更稳视角

### 当前状态

- 正在对 `view 5` 单独导出 baseline / no-anchor / anchor 的 phase0 渲染结果

## 2026-03-09 13:20:00 UTC

### 关键口径纠偏: 当前 A/B 尚未接入 FlashVSR-Pro 6 路 SR reference

### 已验证事实

- 当前已完成的 depth-anchor A/B 使用的是:
  - `--pose-root assets/demo/static/diffusion_output_generated`
  - `--intrinsics-root assets/demo/static/diffusion_output_generated`
  - `--rgb-root assets/demo/static/diffusion_output_generated`
- 这些 run 没有传:
  - `--reference-root outputs/flashvsr_reference/full_scale2x`
  - `--reference-mode super_resolved`
  - `--sr-scale 2.0`
- 当前工作区下的 `outputs/flashvsr_reference` 为空, 文档里提到的 `full_scale2x/<view>/rgb/00172.mp4` 也不存在

### 影响

- 目前得到的结论只适用于 native reference 口径
- 还不能拿来回答“用了 FlashVSR-Pro 6 视频以后, depth anchor 是否更有帮助”

### 当前状态

- 正在全盘搜索这台机器上是否已有其它位置的 FlashVSR reference 产物

## 2026-03-09 13:50:00 UTC

### 新阶段: 重新生成 FlashVSR-Pro 6 路 SR reference

### 目标

- 用 `/workspace/FlashVSR-Pro` 重新生成 `00172` 的 6 路 2x SR 视频
- 后续把 `refine_robust_v2` 的 reference supervision 切到真实 external SR 口径
- 再重新做 depth-anchor A/B, 避免 native 与 SR 口径混淆

### 阶段

- [ ] 阶段SR1: 跑通单路 FlashVSR-Pro 实测 smoke
- [ ] 阶段SR2: 扩展到全部 6 路视频生成
- [ ] 阶段SR3: 用 `reference-root` 重新跑 refine_robust_v2 A/B
- [ ] 阶段SR4: 回到侧视角/厚化问题做对比结论

### 已验证前置条件

- `/workspace/FlashVSR-Pro` 仓库与模型文件存在
- 当前 `pixi` Python 已补齐 `ftfy`、`datasets`、`ffmpeg-python`、`pytorch-lightning`、`torchsde`
- `block_sparse_attn` 已按 `BLOCK_SPARSE_ATTN_CUDA_ARCHS=80` 编译成功
- `infer.py --help` 已在当前环境下成功启动

### 当前状态

- 正在先跑 `view 5 / 00172` 的单路 FlashVSR-Pro smoke

## 2026-03-09 14:00:00 UTC

### 阶段SR1 完成更新

- [x] 阶段SR1: 跑通单路 FlashVSR-Pro 实测 smoke
- [ ] 阶段SR2: 扩展到全部 6 路视频生成
- [ ] 阶段SR3: 用 `reference-root` 重新跑 refine_robust_v2 A/B
- [ ] 阶段SR4: 回到侧视角/厚化问题做对比结论

### 已验证结果

- `view 5 / scene 00172` 已成功生成:
  - `outputs/flashvsr_reference/full_scale2x/5/rgb/00172.mp4`
- `scripts/run_flashvsr_reference.py` 本地 runner 已成功落盘 manifest 和汇总 JSON
- 下一步可以安全扩展到剩余 `0,1,2,3,4` 五路

### 当前状态

- 正在启动剩余 5 路 FlashVSR-Pro 生成

## 2026-03-09 14:15:00 UTC

### 阶段SR2 完成更新

- [x] 阶段SR1: 跑通单路 FlashVSR-Pro 实测 smoke
- [x] 阶段SR2: 扩展到全部 6 路视频生成
- [ ] 阶段SR3: 用 `reference-root` 重新跑 refine_robust_v2 A/B
- [ ] 阶段SR4: 回到侧视角/厚化问题做对比结论

### 已完成结果

- 已成功生成 `00172` 的 6 路 2x SR 视频:
  - `outputs/flashvsr_reference/full_scale2x/0/rgb/00172.mp4`
  - `outputs/flashvsr_reference/full_scale2x/1/rgb/00172.mp4`
  - `outputs/flashvsr_reference/full_scale2x/2/rgb/00172.mp4`
  - `outputs/flashvsr_reference/full_scale2x/3/rgb/00172.mp4`
  - `outputs/flashvsr_reference/full_scale2x/4/rgb/00172.mp4`
  - `outputs/flashvsr_reference/full_scale2x/5/rgb/00172.mp4`
- 各 view manifest 与总汇总 JSON 已落盘

### 新的 A/B 口径

- 共同参数:
  - `reference-root outputs/flashvsr_reference/full_scale2x`
  - `reference-mode super_resolved`
  - `sr-scale 2.0`
  - `stage2a-mode enhanced`
  - `patch-size 256`
  - `lambda-patch-rgb 0.25`
  - `lambda-sampling-smooth 0.0005`
  - `enable-pruning`
  - `target_subsample 8`
  - `iters_stage2a 20`
- 对照:
  - no-anchor
- 实验:
  - `enable-depth-anchor --lambda-depth-anchor 0.05`

### 当前状态

- 正在启动 external SR 口径下的 no-anchor 基线 run

## 2026-03-09 14:25:00 UTC

### 阶段SR3 / 阶段SR4 完成更新

- [x] 阶段SR1: 跑通单路 FlashVSR-Pro 实测 smoke
- [x] 阶段SR2: 扩展到全部 6 路视频生成
- [x] 阶段SR3: 用 `reference-root` 重新跑 refine_robust_v2 A/B
- [x] 阶段SR4: 回到侧视角/厚化问题做对比结论

### 最终结论摘要

- `FlashVSR-Pro` 6 路 2x reference 已经真实接入 `refine_robust_v2`
- external SR 口径本身有明显提升:
  - full-view no-anchor: `PSNR 17.8840 -> 23.9982`
  - `view 5` side eval: `PSNR 17.9264 -> 24.5583`
- 但在当前最佳候选 `lambda_depth_anchor=0.05` 下:
  - full-view SR 最终仍低于 no-anchor:
    - `23.7307 < 23.9982`
  - `view 5` 侧视轨迹 121/121 帧全部低于 no-anchor
  - 说明目前没有证据支持“depth anchor 在真实 SR 口径下改善了侧视厚化”

### 当前状态

- 本轮验证闭环已经完成, 正在整理交付说明和后续建议

## 2026-03-09 14:35:00 UTC

### 新任务: 盘点 `docs/plans/2026-03-06-long-lrm-style-post-refinement.md` 尚未完成项

### 目标

- 通读 `docs/plans/2026-03-06-long-lrm-style-post-refinement.md`
- 区分:
  - 已完成
  - 明确未完成
  - 因 `depth anchor` 弃用而应视为失效/待清理
- 给出一份可执行的“剩余事项清单”

### 阶段

- [ ] 阶段P1: 提取文档中的任务清单、勾选框和状态段落
- [ ] 阶段P2: 结合当前仓库状态判断哪些项已完成
- [ ] 阶段P3: 标出受 `depth anchor` 弃用影响的失效项
- [ ] 阶段P4: 输出最终剩余事项列表

### 当前状态

- 正在读取计划文档并提取未完成项

## 2026-03-09 14:31:34 UTC

### 阶段P1-P4 完成更新

- [x] 阶段P1: 提取文档中的任务清单、勾选框和状态段落
- [x] 阶段P2: 结合当前仓库状态判断哪些项已完成
- [x] 阶段P3: 标出受 `depth anchor` 弃用影响的失效项
- [x] 阶段P4: 输出最终剩余事项列表

### 盘点结论

- 这份文档是"当前状态 + continuation tasks + 历史 0-to-1 拆解"混合文档, 不能把 `Task 1 ~ Task 10` 直接当成今天的未完成清单
- 当前最明确仍未彻底完成的是:
  - `Continuation Task C`
  - `Closest-to-SplatSuRe Track`
- `Continuation Task A / B / D` 不属于"主能力没落地", 更准确地说是:
  - A: selective SR 默认参数仍待优化
  - B: full-view 基线仍待文档和更长 smoke 收口
  - D: `Stage 2B` 的 gate / warm-start / 默认策略仍待收口
- 用户已明确 `depth anchor` 弃用
  - 本文档当前 continuation 主线并不依赖它
  - 后续不应再把它算进这份计划的剩余主任务

### 当前状态

- 本轮计划盘点已完成, 正在整理面向用户的最终未完成项清单

## 2026-03-09 14:38:00 UTC

### 新任务: 重构 `2026-03-06-long-lrm-style-post-refinement.md` 的阅读结构

### 目标

- 不改变文档中已经沉淀的技术内容
- 只重构"如何阅读这份文档"的结构提示
- 明确区分:
  - 当前 backlog
  - 已完成但仍可继续优化
  - 高侵入未来路线
  - 历史归档任务
  - 已不再继续的方向

### 阶段

- [ ] 阶段D1: 在文档前部补一个当前阅读指南
- [ ] 阶段D2: 给容易误导的章节补明确标题和状态
- [ ] 阶段D3: 校对文档内部口径并输出总结

### 当前状态

- 正在准备对计划文档做最小但高价值的结构收口

## 2026-03-09 14:45:00 UTC

### 阶段D1-D3 完成更新

- [x] 阶段D1: 在文档前部补一个当前阅读指南
- [x] 阶段D2: 给容易误导的章节补明确标题和状态
- [x] 阶段D3: 校对文档内部口径并输出总结

### 已完成内容

- 在计划文档前部新增 `Reader Guide / Current Backlog Snapshot`
- 明确标出:
  - 当前最明确未完成的是 `Continuation Task C`
  - `Continuation Task B / D / A` 是已落地后的继续优化
  - `Task 1 ~ Task 10` 不是当前 backlog
  - `depth anchor` 当前已弃用, 不纳入这份计划的 continuation backlog
- 给 `Continuation Task C` 补了显式状态说明
- 把历史章节和当前顺序章节标题改成更不易误读的形式

### 当前状态

- 文档第一轮结构收口已完成, 正在准备向用户汇报修改结果和后续建议

## 2026-03-09 14:55:00 UTC

### 新任务: 落地 `Continuation Task C` 的 usage / diagnostics / 产物说明统一

### 目标

- 统一 `docs/cmd.md`、主 usage 文档、Task A runbook 对阶段命名的解释
- 消除 `stop_after=stage2a` 与 `phase_reached=stage3sr` 这类看似矛盾的地方
- 把常见产物和内部子阶段的对应关系写清楚

### 阶段

- [ ] 阶段C1: 统一命名约定和阶段解释
- [ ] 阶段C2: 统一 `phase_reached` / `stop_after` 语义
- [ ] 阶段C3: 统一 diagnostics / 产物对照说明
- [ ] 阶段C4: 校对并回写记录

### 计划修改文件

- `docs/cmd.md`
- `docs/joint_refinement_camera_gaussians_v2_usage.md`
- `docs/2026-03-06-rtx4090-baseline-task-a-runbook.md`

### 当前状态

- 正在对这 3 份文档做定向收口

## 2026-03-09 15:11:02 UTC

### 阶段C1-C4 完成更新

- [x] 阶段C1: 统一命名约定和阶段解释
- [x] 阶段C2: 统一 `phase_reached` / `stop_after` 语义
- [x] 阶段C3: 统一 diagnostics / 产物对照说明
- [x] 阶段C4: 校对并回写记录

### 已完成内容

- `docs/cmd.md`
  - 新增命名约定速记
  - 明确 `stage2a` 与 `Stage 3A / Phase 3S / Stage 3SR` 的关系
  - 修正 full-view `sub16` smoke 命令的 observation 口径
- `docs/joint_refinement_camera_gaussians_v2_usage.md`
  - 新增 `误区 5`
  - 新增 `phase_reached` 解释
  - 扩展 `Stage 2A` 映射表
  - 新增产物与出现条件表
- `docs/2026-03-06-rtx4090-baseline-task-a-runbook.md`
  - 新增阶段命名与成功信号约定
  - 明确 `--stop-after stage2a` 与 `phase_reached = stage3sr` 不矛盾

### 验证结果

1. `git diff --check -- docs/cmd.md docs/joint_refinement_camera_gaussians_v2_usage.md docs/2026-03-06-rtx4090-baseline-task-a-runbook.md`
   - 通过, 无格式错误
2. `git diff -- ...`
   - 已确认 3 份文档都按预期补入命名、诊断和产物说明
3. `rg -n -- '--target-subsample 8|full_view_.*sub16|phase_reached = stage3sr|phase_reached = stage2a' docs/cmd.md`
   - 已确认 `sub16` 命令未再残留 `target_subsample=8`

### 当前状态

- `Continuation Task C` 的第一轮文档统一已完成, 正在整理最终交付说明

## 2026-03-09 15:18:00 UTC

### 新任务: 收口 `Continuation Task B` 的 full-view baseline 文档口径

### 目标

- 把 full-view baseline 的正式推荐顺序写清楚
- 明确:
  - 48G 主机当前只推荐 `target_subsample=16`
  - native smoke 是正式起点
  - SR smoke 是同预算下的增强分支
  - 更长 smoke / `Stage 2B` 仍属于后续工作

### 阶段

- [ ] 阶段B1: 统一 full-view baseline checklist
- [ ] 阶段B2: 统一 full-view smoke 命令和观察重点
- [ ] 阶段B3: 在计划文档里刷新 `Continuation Task B` 状态
- [ ] 阶段B4: 校对并回写记录

### 计划修改文件

- `docs/cmd.md`
- `docs/joint_refinement_camera_gaussians_v2_usage.md`
- `docs/plans/2026-03-06-long-lrm-style-post-refinement.md`

### 当前状态

- 正在把 full-view baseline 的正式使用口径收成一版清晰 checklist

## 2026-03-09 15:25:00 UTC

### 阶段B1-B4 完成更新

- [x] 阶段B1: 统一 full-view baseline checklist
- [x] 阶段B2: 统一 full-view smoke 命令和观察重点
- [x] 阶段B3: 在计划文档里刷新 `Continuation Task B` 状态
- [x] 阶段B4: 校对并回写记录

### 已完成内容

- `docs/cmd.md`
  - 新增 full-view 正式推荐顺序
  - 明确 native smoke 是正式 baseline 起点
  - 明确 SR smoke 是同预算下的增强分支
- `docs/joint_refinement_camera_gaussians_v2_usage.md`
  - 在 full-view root mode 章节加入正式推荐顺序
  - 明确不建议在 baseline 未固定前直接混入 `Stage 2B`
  - 在末尾 full-view 命令块补充其职责说明
- `docs/plans/2026-03-06-long-lrm-style-post-refinement.md`
  - 为 `Continuation Task B` 增加 `2026-03-09 refresh`
  - 把当前剩余工作收敛为:
    - 更长 smoke
    - `48 observations` 档位下继续比较 native vs SR
    - 需要时再衔接 `Stage 2B`

### 验证结果

1. `git diff --check -- docs/cmd.md docs/joint_refinement_camera_gaussians_v2_usage.md docs/plans/2026-03-06-long-lrm-style-post-refinement.md`
   - 通过, 无格式错误
2. 已人工复核:
   - `docs/cmd.md`
   - `docs/joint_refinement_camera_gaussians_v2_usage.md`
   - `docs/plans/2026-03-06-long-lrm-style-post-refinement.md`
   三处对 full-view baseline 的正式口径一致

### 当前状态

- `Continuation Task B` 的第一轮文档收口已完成, 当前剩余重点已缩小到更长 smoke 和后续 `Stage 2B`

## 2026-03-09 15:32:00 UTC

### 新任务: 按收口后的 full-view baseline 口径继续做更长 smoke

### 目标

- 按当前正式口径继续验证 `Continuation Task B` 的剩余项
- 在 `48 observations` 档位下运行更长一点的:
  - full-view native smoke
  - full-view SR smoke
- 判断是否已经具备继续进入 `Stage 2B` 的依据

### 阶段

- [ ] 阶段S1: 盘点现有 full-view 输出与可复用命令
- [ ] 阶段S2: 运行 full-view native 更长 smoke
- [ ] 阶段S3: 运行 full-view SR 更长 smoke
- [ ] 阶段S4: 汇总 native vs SR 结果并判断下一步

### 当前状态

- 正在盘点现有 full-view 输出和这轮更长 smoke 的推荐参数

## 2026-03-09 15:41:30 UTC

### 中断恢复: 继续补 full-view SR reference

### 说明

- 上一轮在执行 6 路 `FlashVSR-Pro` 批处理时被用户主动中断
- 当前优先做:
  - 核对已成功 / 未完成 / 可能损坏的 reference 文件
  - 只补跑缺失部分

### 当前状态

- 正在核对 `outputs/flashvsr_reference` 的实际落盘状态

## 2026-03-09 15:57:00 UTC

### 用户口径更新

- 用户明确要求:
  - 后续实验统一使用 `--target-subsample 8`
- 这会覆盖本轮原先准备按 `sub16` 继续做更长 smoke 的执行口径
- 当前文档里的 `48G -> sub16` 仍保留为文档基线
- 但当前 A800 实验线从现在起统一切到:
  - `target_subsample=8`

### 当前状态

- 正在切换到 `sub8` 口径, 准备核对 SR reference 完整性并继续 native / SR 长 smoke

## 2026-03-09 16:18:00 UTC

### `sub8` 长 smoke 结论

- native `Stage 2A`:
  - `PSNR = 22.3538`
  - `residual_mean = 0.04232`
- SR `Stage 3SR`:
  - `PSNR = 23.9967`
  - `residual_mean = 0.03452`
- 结论:
  - 在用户指定的 `target_subsample=8` 口径下
  - SR 分支已经明显领先当前 native baseline
  - 因此下一步不再停在 smoke, 直接继续验证 `Stage 2B` warm-start

### 当前状态

- 正在读取现有 `Stage 2B` warm-start 命令口径, 准备从 SR `gaussians_stage3sr.ply` 继续

## 2026-03-09 16:22:00 UTC

### 新阶段: 从 `sub8` 的 SR 产物继续验证 `Stage 2B`

### 目标

- 把当前更强的:
  - `outputs/refine_v2/full_view_sr_stage3sr_long_sub8_iter20_20260309`
  作为 warm-start 起点
- 继续跑 `Stage 2B`
- 判断:
  - `SR stage3sr -> Stage 2B` 在当前 `sub8` 口径下是否继续提升

### 执行口径

- 保留:
  - `target_subsample=8`
  - `enable_pruning`
  - `resume + start-stage stage2b`
- `Stage 2B` 主损失继续对 native `gt_images`
- 不把这轮续跑重新解释成"继续 SR patch supervision"

### 当前状态

- 正在复制 SR baseline 目录, 准备起 `Stage 2B` warm-start

## 2026-03-09 16:30:00 UTC

### 继续执行: `sub8` 口径下的 `SR -> Stage 2B` warm-start

### 说明

- 已重新阅读六文件尾部, 确认上一轮已经完成:
  - full-view native `Stage 2A` 长 smoke
  - full-view SR `Stage 3SR` 长 smoke
- 用户最新口径继续有效:
  - 后续统一使用 `--target-subsample 8`
  - `depth anchor` 不再纳入当前主线
- 因此当前最直接的下一步是:
  - 从 `full_view_sr_stage3sr_long_sub8_iter20_20260309` 的 `gaussians_stage3sr.ply`
  - 继续验证 `Stage 2B` warm-start

### 当前状态

- 正在核对 `Stage 2B` 命令参数与输出目录, 随后直接起跑

## 2026-03-09 16:33:00 UTC

### 执行动作: 复制 `SR stage3sr` 输出目录, 准备在副本上继续 `Stage 2B`

### 说明

- 为避免覆盖已经完成的 `Stage 3SR` baseline
- 这轮 `Stage 2B` 续跑使用独立输出目录:
  - `outputs/refine_v2/full_view_stage2b_from_sr_stage3sr_sub8_iter20_20260309`
- 复制内容包含:
  - `state/latest.pt`
  - `gaussians_stage3sr.ply`
  - 既有 diagnostics / metrics / videos

### 当前状态

- 目录复制完成后, 立即以 `resume + start-stage stage2b + target_subsample=8` 起跑

## 2026-03-09 16:34:00 UTC

### 执行调整: 改用新的唯一输出目录继续 `Stage 2B`

### 现象

- 原计划里想先删除同名旧目录再复制
- 但 `rm -rf` 被当前终端策略拦截

### 处理

- 不做破坏性删除
- 直接改用新的输出目录:
  - `outputs/refine_v2/full_view_stage2b_from_sr_stage3sr_sub8_iter20_20260309_run2`

### 当前状态

- 正在完成目录复制, 随后直接启动 `Stage 2B` 续跑

## 2026-03-09 16:38:00 UTC

### 起跑前参数确认: `SR stage3sr -> Stage 2B`

### 命令口径

- 输入目录:
  - `outputs/refine_v2/full_view_stage2b_from_sr_stage3sr_sub8_iter20_20260309_run2`
- 关键参数:
  - `reference-root outputs/flashvsr_reference/full_scale2x`
  - `reference-mode super_resolved`
  - `sr-scale 2.0`
  - `target-subsample 8`
  - `start-stage stage2b`
  - `enable-stage2b`
  - `resume`
  - `stop-after stage2b`
- 第一段 `Stage 2B` 先使用:
  - `iters-stage2b 80`

### 为什么先跑 80 iter

- `Stage 2B` 当前是固定迭代, 不会像前面的 smoke 那样自动 plateau 停止
- 先用一段更长但可控的迭代数看趋势
- 如果末段还在明显改善, 再基于同一目录继续追加下一段

### 当前状态

- 正在启动 `Stage 2B` 第一段 80 iter 验证

## 2026-03-09 16:27:30 UTC

### 动态验证更新: 启动慢不等于流程卡死

### 现象

- `Stage 2B` 续跑启动后数分钟内仍没有 `metrics_stage2b.json`

### 已验证证据

- 运行中的 Python 进程持续高占用
- 中断栈明确落在:
  - `run_phase1_prepare_weights`
  - `weight_builder.build_weight_map`
  - `torch.quantile`
- `state/latest.pt` 已被刷新, 说明前面阶段确实在执行

### 结论

- 上一轮“可能卡在启动”的假设已被推翻
- 当前真实情况是:
  - full-view `sub8` 的 `Phase 1` 前置权重构造比较慢
  - 需要给它更长的无输出等待时间

### 当前状态

- 准备重新启动同一命令, 但这次按更长窗口等待 `Phase 1` 结束

## 2026-03-09 16:45:00 UTC

### 阻塞修复完成: mixed-device patch intrinsics

### 已验证结论

- 根因不是 `Stage 2B` 本体, 也不是 resume 失效
- 真正根因是:
  - `build_patch_intrinsics()` 只对齐 dtype, 没对齐 device
  - 当 CPU patch windows 遇到 CUDA scene 时, 会在 warm-start 路径直接报错
- 已补最小回归测试并通过

### 当前状态

- 正在重新启动 full-view `SR -> Stage 2B` 真实命令, 继续验证指标是否提升

## 2026-03-09 16:57:00 UTC

### 结果补强: 增加 `view 5` 侧视角 phase0 对比

### 为什么要补这一步

- 用户已经明确指出:
  - 正面视角可能看不出厚化/重叠
  - 一转到侧面更容易暴露几何问题
- 因此仅有 full-view 平均指标还不够
- 需要把:
  - `stage3sr`
  - `stage2b`
  的 `view 5` 单视角结果单独拉出来比较

### 当前状态

- 正在核对 `view 5` 直连资产路径, 随后用 `--dry-run` 跑两条 phase0 侧视评估

## 2026-03-09 17:03:00 UTC

### 阶段完成更新: `SR stage3sr -> Stage 2B` 已完成真实验证

- [x] 刷新计划与记录, 确认 `sub8` `Stage 2B` 续跑口径
- [x] 执行 `SR -> Stage 2B` warm-start 实验并完成阻塞修复
- [x] 提取 full-view / `view 5` 指标并与 `stage3sr` 对比
- [x] 回写六文件, 记录结论与后续建议

### 最终结论

- 在用户要求的 `--target-subsample 8` 口径下:
  - `SR stage3sr -> Stage 2B` 有明确提升
- full-view:
  - `PSNR 23.9967 -> 25.4044`
  - `residual_mean 0.03452 -> 0.02871`
  - `sharpness 0.002959 -> 0.004525`
- `view 5` 侧视角:
  - `PSNR 24.5507 -> 25.5023`
  - `residual_mean 0.03379 -> 0.02953`
  - `sharpness 0.002947 -> 0.004487`

### 过程中确认的真实 blocker

- mixed-device bug 1:
  - `build_patch_intrinsics()` 没把 CPU patch window offset 迁到 CUDA intrinsics 设备
- mixed-device bug 2:
  - warm-start 丢失 `sr_selection_map` 时, fallback `ones_like(robust_patch)` 留在 CPU
- 两个 blocker 都已修复, 并各自补了 CUDA 回归测试

### 当前状态

- 这轮验证主目标已完成
- 下一步可继续二选一:
  - 基于当前 `run2` 再追加一段 `Stage 2B` iter
  - 补 `native -> Stage 2B` 的 `sub8` 对照

## 2026-03-09 17:10:00 UTC

### 用户口径更新

- 用户明确要求:
  - 继续 `Stage 2B` 后面的任务
  - 跳过 `native -> Stage 2B` 的 `sub8` 对照
- 因此当前主线调整为:
  - 先核对当前 `run2` 在 `Stage 2B` 结束后的真实 gate 状态
  - 若 gate 允许, 直接继续后续阶段
  - 若 gate 不允许, 也要用真实证据说明为什么当前流程自然止步

### 当前状态

- 正在读取 `state/latest.pt` 的 diagnostics_state, 确认 `Phase 3 / Phase 4` gate

## 2026-03-09 17:14:00 UTC

### 执行动作: 以 `run2` 为起点继续追加一段 `Stage 2B`, 同时打开后续 fallback gate

### 说明

- 当前没有 `start_stage=stage2b` 之后的独立 CLI 入口
- 因此要继续验证 `Stage 2B` 后面会不会真正进入:
  - `Phase 3` pose diagnostic
  - `Phase 4` joint fallback
- 需要做的是真实地从 `run2` 继续一段 `Stage 2B`, 但不再 `stop-after stage2b`
- 为避免污染 `run2`, 使用新目录:
  - `outputs/refine_v2/full_view_stage2b_plus_post_from_sr_sub8_iter160_20260309_run3`

### 当前状态

- 正在复制 `run2`, 随后启动 `run3`

## 2026-03-09 17:22:00 UTC

### 结果补强: 再补 `run3` 的 `view 5` 侧视角评估

### 目的

- 用户明确更关心侧视角暴露出来的厚化/重叠
- 因此除了 full-view 平均指标, 还要确认:
  - `run2 -> run3` 的继续追加 `Stage 2B`
  - 是否在 `view 5` 上也同步改善

### 当前状态

- 正在运行 `run3 gaussians_stage2b.ply` 的 `view 5 --dry-run`

## 2026-03-09 17:28:00 UTC

### 口径纠偏后继续: 转入 `Continuation Task A`

### 现象

- 用户已明确澄清:
  - 继续的是 backlog 后续任务
  - 不是继续追加 `Stage 2B` 训练
- 当前 backlog 顺序显示:
  - `B -> D -> A -> Closest-to-SplatSuRe Track`
- 因此在 `D` 这轮 SR `Stage 2B` 收口之后
- 现在应当转入 `Continuation Task A`

### 当前主假设

- 当前 SR 路线没有完全吃满 external SR 的价值
- 更可能的首个主因不是 `L_sampling_smooth`
  - 因为它当前有效权重很小
- 而是 selective SR patch supervision 的实际强度仍偏保守
  - 当前 `selection_mean = 0.1456`
  - `selection_max = 0.25`
  - 同时 `loss_patch_rgb ≈ 0.005`
- 因此先做一个最小可证伪实验:
  - 单变量把 `lambda_patch_rgb: 0.25 -> 0.5`
  - 其它参数保持不变

### 当前状态

- 正在启动 `Continuation Task A` 的第一条单变量 full-view SR 实验

## 2026-03-09 17:40:00 UTC

### `Continuation Task A` 第一条假设回收

### 已验证现象

- 单变量调整:
  - `lambda_patch_rgb: 0.25 -> 0.5`
- full-view 结果:
  - `PSNR -0.0196`
  - `residual_mean +0.000155`
- `view 5` 结果:
  - `PSNR -0.0298`
  - `residual_mean +0.000141`
- 同时关键诊断几乎不变:
  - `selection_mean 0.145594 -> 0.145587`
  - `selection_max` 仍是 `0.25`

### 结论

- 当前主假设“只是 patch RGB 权重偏低”不成立
- 更像是真正的瓶颈还在:
  - `W_sr_select` 本身的保守程度
  - 或 pruning / opacity 对 selection 权重的压制

### 下一步

- 继续 `Continuation Task A` 的第二条最小可证伪实验:
  - 保持 `lambda_patch_rgb = 0.25`
  - 保持 `lambda_sampling_smooth = 0.0005`
  - 只取消 pruning

### 当前状态

- 正在启动 `no-pruning` 的 full-view SR 对照

## 2026-03-09 17:48:00 UTC

### `Continuation Task A` 转入 `W_sr_select` 本体调整

### 已验证现象

- 两条表层超参数实验都没让 SR baseline 变好:
  - `lambda_patch_rgb 0.25 -> 0.5`
  - `no-pruning`
- 共同特征是:
  - `selection_mean` 基本不动
  - `selection_max` 始终卡在 `0.25`

### 当前主假设

- 瓶颈不在 patch loss 比例本身
- 更像是 `W_sr_select` 动态范围过弱
- 因此最小实现改动改为:
  - 对每个 view 的非零 selection map 做 unit-peak 归一化
  - 保留空间分布, 只放大动态范围

### 已完成验证

- 新增单测 `test_sr_selection_weight_normalizes_nonzero_views_to_unit_peak`
  - 修复前失败
  - 修复后通过
- 相关 `phase3s / stage3sr` patch supervision 测试通过

### 当前状态

- 正在用默认 SR baseline 参数重跑 full-view `sub8` 实验, 验证这次代码改动的真实收益

## 2026-03-10 00:05:30 UTC

### `Continuation Task A` 当前阶段结论

- 已验证三条最小方向:
  - 提高 `lambda_patch_rgb`
  - 关闭 pruning
  - naive 放大 `W_sr_select` 动态范围
- 三条都没有让 full-view SR baseline 变好
- 因此当前不再继续盲扫 `Task A` 的表层超参数
- 下一步转入:
  - `Closest-to-SplatSuRe Track Phase A`
  - 先做更真实的 fidelity score 方案预研与入口设计

### 当前状态

- 正在阅读 `specs/long_lrm_style_post_refinement.md` 中 Phase A 的 fidelity 公式与数据需求

## 2026-03-10 02:53:49 UTC

### 执行动作: 收口 `Continuation Task A`, 切换到 `Closest-to-SplatSuRe Track Phase A`

### 说明

- 上一轮已经用真实 full-view `sub8` 证据把 `Task A` 收缩到很窄:
  - 提高 `lambda_patch_rgb`
  - 关闭 pruning
  - naive 放大 `W_sr_select` 峰值
- 这三条都没有让 SR baseline 变好.
- 用户也已经明确:
  - 后续统一使用 `--target-subsample 8`
  - 不再额外补 `view 5` 侧面评估
  - 现在要继续 backlog 后面的任务, 不是继续 `Stage 2B` 训练
- 因此当前最值得做的不是继续盲扫表层超参数,
  而是把失败证据写回六文件, 然后直接转入 `Closest-to-SplatSuRe Track Phase A`.

### 当前最小实现目标

- 先不碰全图 HR render 或 Phase B.
- 先把 `phase3s` 的 fidelity 从“单轮 render meta 代理”推进到更接近文档描述的跨视图统计:
  - `r_min`
  - `r_max`
  - `argmax_view`
  - `num_times_seen`
  - `rho = r_max / r_min`
- 并保持当前 `Stage 3SR` 还能继续消费 `sr_selection_map`.

### 当前状态

- 正在补写 `WORKLOG.md / EPIPHANY_LOG.md`, 随后继续阅读 `weight_builder.py / runner.py / tests` 的 Phase A 入口.

## 2026-03-10 03:04:07 UTC

### 阶段完成: `Closest-to-SplatSuRe Track Phase A` 第一轮最小落地已完成

### 已完成内容

- 先写失败测试, 锁住 `Phase A` 需要的新行为:
  - 跨视图 `r_min / r_max / rho / num_times_seen`
  - `max_view_mask` 参与 selection map
  - `phase3s` 产物新增 `rho_mean / num_times_seen_mean / max_view_counts`
- 已在代码中落地:
  - `WeightBuilder.compute_gaussian_fidelity_diagnostics()`
  - `compute_gaussian_fidelity_score()` 兼容 wrapper
  - `build_sr_selection_weight(..., fidelity_diagnostics=...)`
  - `runner.run_phase3s_build_sr_selection()` 对新的 diagnostics 落盘
  - `DiagnosticsWriter.write_gaussian_fidelity_summary()` 写出额外统计
- 已完成回归验证:
  - `tests/refinement_v2/test_weight_builder.py tests/refinement_v2/test_patch_supervision.py -k "fidelity or max_view"`
  - `5 passed`
  - `tests/refinement_v2/test_weight_builder.py tests/refinement_v2/test_losses.py tests/refinement_v2/test_patch_supervision.py tests/refinement_v2/test_runner_phase0.py tests/refinement_v2/test_runner_stage2b.py`
  - `35 passed`

### 当前状态

- `Closest-to-SplatSuRe Track Phase A` 的代码级最小闭环已完成
- 下一步若继续本主线, 应优先做:
  - 用新 fidelity 口径重跑 full-view `sub8` 实验
  - 再决定是否继续推进 `Phase B`

## 2026-03-10 03:05:30 UTC

### 执行动作: 用新的 `Phase A` fidelity 口径重跑 full-view `sub8` Stage 3SR

### 说明

- `Closest-to-SplatSuRe Track Phase A` 的代码级最小实现已经完成并通过回归.
- 按 backlog 的自然下一步, 现在不能停在“测试转绿”.
- 必须回到真实 full-view `sub8` 口径, 验证新的:
  - `rho`
  - `num_times_seen`
  - `max_view_mask`
  - selection routing
  是否真的让 SR 主线比旧 baseline 更好.
- 用户已经明确后续不再补侧视角, 因此这轮只看 full-view.

### 当前状态

- 正在从旧 baseline 目录和文档里恢复命令口径, 随后启动新的 `stage3sr` 实验目录.

## 2026-03-10 03:11:30 UTC

### 实验完成: 新 `Phase A` 口径的 full-view `sub8` `stage3sr` 已完成真实重跑

### 输出目录

- `outputs/refine_v2/full_view_sr_stage3sr_phaseA_rho_sub8_iter20_20260310`

### 已观察到的现象

- 相对旧 baseline `outputs/refine_v2/full_view_sr_stage3sr_long_sub8_iter20_20260309`:
  - `PSNR: 23.996732 -> 24.000970` (`+0.004238`)
  - `residual_mean: 0.03452381 -> 0.03442931` (`-0.0000945`)
  - `sharpness: 0.00295948 -> 0.00295518` (`-0.00000430`)
- `Phase 3S` 诊断发生了明显结构变化:
  - `selection_mean: 0.145594 -> 0.015739`
  - `selection_max: 0.25 -> 0.995640`
  - `fidelity_mean: 0.044283 -> 0.361312`
  - 新增 `rho_mean = 0.960463`
  - 新增 `num_times_seen_mean = 52.8679`
- `Stage 3SR` 的 patch 相关损失显著下降:
  - `loss_patch_rgb: 0.00500855 -> 0.000642605`
  - `loss_sampling_smooth: 0.00498783 -> 0.000733979`

### 当前结论

- 这轮 `Phase A` 最小实现已经显著改变了 selection 分布与诊断形态
- 但它还没有把 full-view `sub8` 的最终 `stage3sr` 指标显著拉高
- 因此当前更像是:
  - 结构语义变得更接近 SplatSuRe 了
  - 但收益还停留在“几乎持平”, 还不能宣称这是明确增益

### 当前状态

- 正在把这轮真实实验结果同步回 `notes.md / WORKLOG.md / docs/plans`
- 随后收口本轮结论, 给出最自然的下一步建议

## 2026-03-10 00:00:00 UTC

### 新任务: 分析 Stage 3SR patch supervision 数据流与最小挂点

### 目标

- 读取 `src/refinement_v2/runner.py` 及相关 helper
- 回答当前 `Stage 3SR` patch supervision 的真实数据流和关键函数
- 判断是否已有可复用的 reference 分辨率整图渲染 / 多 patch 渲染入口
- 基于现有结构, 找出升级为多 patch / 更大覆盖 weighted SR supervision 的最小改动挂点

### 阶段

- [ ] 阶段1: 读取 runner 和 helper, 画出 Stage 3SR patch supervision 调用链
- [ ] 阶段2: 盘点现有 reference 分辨率渲染与 patch 渲染入口
- [ ] 阶段3: 归纳最小挂点并整理文件行号证据
- [ ] 阶段4: 记录结论并准备交付

### 当前状态

- 正在读取 `runner.py` 与 patch 相关 helper, 准备做静态链路分析
